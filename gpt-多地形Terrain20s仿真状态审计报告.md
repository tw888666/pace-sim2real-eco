# PACE-ECO 多地形 Terrain20s 仿真状态审计报告

> 已归档：本报告只审计v1.3的40×10 m、5×20网格，不能放行v1.4 Terrain20sWide。

协议版本：`gpt-multi-terrain-v1.3`

执行时间：2026-08-06 20:19–20:21 CST

## 1. 结论与边界

经 nullptr 明确授权，使用 GPU0 依次审计 flat、rough、stairs、boxes、slope 五类实际
Isaac Sim（机器人仿真平台）碰撞网格。五类结果均为“全部通过”，退出码均为 0。

每类生成 5×20=100 个实际地形实例，并使用 399900 条射线覆盖局部前向位移
`[-2,30] m`、横向位移 `[-3,3] m`。五类合计审计 500 个实例、1999500 条碰撞网格
射线。审计只构造环境并推进首次物理步，不加载、不训练、不生成或修改模型权重，也不是
calibration（校准）或 holdout（留出集）正式评估。

## 2. 全部通过的检查

五类均通过以下 11 项总检查：

- 首次物理步无意外终止，奖励和 actor/critic 观察全部为有限值；
- 实际地形 origin（原点）形状为 5×20，列类别与方向元数据正确；
- 出生中心的实际碰撞面高度与 origin 高度一致，未发现出生点穿透；
- 真实碰撞网格覆盖出生点、29 m 有效几何段、29 m 后有限平台及 30 m 审计线；
- 难度按五行分层，且同列随行号严格递增；
- 没有性能驱动课程项；
- 终止项恰为历史 `time_out/base_contact`，未混入完成、侧向或后退终止。

分类检查还确认：

- flat、rough、boxes 的起点与终端净高差符合 level（平向）定义；
- stairs 和 slope 各含 50 个 up（上行）与 50 个 down（下行）实例，方向符号正确；
- 楼梯和斜坡的有效段按方向单调，29 m 后均接有限平台，没有无限楼梯或无限斜坡；
- 箱块没有超出冻结的有效几何段和横向安全范围；
- 40×10 m 块内，从出生点前进 30 m、后退 2 m 或侧移 3 m 仍分别保留
  5 m、3 m、2 m 边界余量，因此审计范围没有进入相邻地形块。

这证明生成后的碰撞几何和运行时状态满足当前冻结边界；它不证明策略在 20 s 内一定不会
异常大幅漂移。v3 calibration/holdout 将失败回合的真实足端越界保留并报告；只在原成功
回合发生真实越界时拒绝整份结果。

## 3. 证据文件

结果根目录：

`logs/supplementary/multi_terrain_v1_3/state_audit/results/`

| 地形 | 地形 seed | 结果文件 | SHA256（安全哈希） |
|---|---:|---|---|
| flat | 910900 | `gpt-flat-仿真状态审计-20260806_201922.json` | `6a75d59565e70e97c766f3de09a89e1d119b1e270e2100bb6600527528f465a0` |
| rough | 911900 | `gpt-rough-仿真状态审计-20260806_201940.json` | `bc5005311ad7cecbf38ca29d1737b2a988ed63e5a64b1c48cb71866bc03f36a8` |
| stairs | 912900 | `gpt-stairs-仿真状态审计-20260806_202013.json` | `390e968306ee5524e2d52f92cbdae85c8d4ec5e9b8fe0f778510d65af8960ba0` |
| boxes | 913900 | `gpt-boxes-仿真状态审计-20260806_202034.json` | `94716d3a9e2edb5e4ad29188e4a6f3a20c02a430287b909e4761b965ff74e0ec` |
| slope | 914900 | `gpt-slope-仿真状态审计-20260806_202055.json` | `99d184d52eb6051ae0089590f5724df8137588151c6d3dc6ae678a2264245a0b` |

启动日志位于：

`logs/supplementary/multi_terrain_v1_3/state_audit/launch/`

## 4. 停止点

Terrain20s 的静态几何门、五类任务型 PPO（近端策略优化）GPU 冒烟门和实际碰撞状态门
现已全部通过，文档同步后的56项非GPU回归测试及静态检查也已通过。没有启动正式训练
或正式评估；当前停止并等待 nullptr 审阅。
