# PACE-ECO 多地形宽场协议修订

日期：2026-08-10
协议：`gpt-multi-terrain-v1.3` → `gpt-multi-terrain-v1.4`
边界审计：`gpt-四足真实地形边界-v3` → `gpt-四足真实地形边界-v4`

## 触发证据

v1.3的40×10 m块相对出生点只有左右±5 m。flat calibration出现按历史定义成功、但
接触足最大侧向位移达到6.440872 m的回合，说明策略能在保持机身坐标系前进速度合格时
转向并进入相邻地形。历史成功定义没有世界坐标航向约束，因此仅把路线预警改为足端记录
不能消除地形污染。

rough v2曾得到200/200回合和29074.5931 J候选，但尚未冻结B_ref。v1.3所有训练、评估和
审计保留原位作为证据，不作为v1.4输入。

## 冻结修改

- 块从40×10 m改为65×60 m，origin从(5,5)改为(30,30) m；
- 有效随机几何仍为出生点向前29 m，末端局部x=59 m，前方留6 m平台；
- 真实相对边缘变为后−30、前+35、左右±30 m；
- 训练从5×20改为5×10=50实例，以控制宽场网格显存；
- 正式评估从单次200环境改为4批×50环境，每批5×10地形并使用独立seed；
- 新任务ID后缀`Terrain20sWide`，新地形seed段和`multi_terrain_v1_4`输出根；
- 边界资格改为任一回合接触足真实越界均硬拒绝，不区分成功和失败；
- 训练检查点评估要求当前实现精确一致，不保留v1.3源码兼容豁免。

## 保持不变

20 s回合、1 m/s指令、成功定义、奖励、终止、观察、动作、网络、4096训练环境、3000次
更新、动力学、摩擦、重置、地形几何范围、前向有效长度、固定权重和PACE-ECO算法均不变。
前30/后2/侧3 m仍是记录用路线预警，不增加方向或边界终止。因此v1.4相对历史Flat主任务
仍只改变地形；扩大承载地形的空域是防止相邻块污染的实验基础设施修正。

## 余量论证

历史成功速度上限1.2 m/s×20 s=24 m。origin到后缘和侧缘均为30 m，扣除24 m名义路程和
1.25 m足端上界后仍有4.75 m。前向按既有30 m路线审计，至前缘剩5 m；有效几何末端至
前缘剩6 m。该论证不能替代GPU实测，正式训练前仍须检查峰值显存、出生穿透、实际碰撞
网格、方向、有限坡/楼梯和边界记录。

## 评估分批不改变统计样本

每批50个环境，批次seed=拆分基准seed+批次索引。全局回合与实例编号用
`批次索引×50+批内编号`映射为0–199，初始状态也按全局编号轮转，因此八个状态仍各25次。
前三批只写隔离暂存JSON；第四批核对四批元数据、检查点哈希、编号、状态和零越界后，才
生成一份200回合正式JSON/CSV。预算和统计工具不会读取批次暂存文件。

## 放行门

必须依次通过CPU几何/单元测试、干净提交、五类GPU冒烟、五类实际碰撞状态审计，才能
重训五类任务型seed0。若65×60 m网格在A100/V100上不可用，保留失败证据并升级协议；
不得回退复用v1.3结果或在看到B_ref/holdout后调整。
