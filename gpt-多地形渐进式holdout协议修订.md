# 多地形渐进式 holdout 协议修订

修订时间：2026-08-17T17:31:58+08:00

## 修订原因

nullptr 在查看任何阶段一正式 holdout（留出集）结果之前，明确要求充分利用空闲 GPU，允许已完成的正式模型先行评估，不再等待 75 个正式终点全部齐备。

## 冻结规则

- 本修订只改变 holdout 授权的时间：由“75 个终点齐备后一次性授权”改为“每批已完成终点生成一个不可覆盖的渐进授权文件”。
- 只授权预注册任务、正式 PPO seed 1–5 和最终 `model_2999.pt`；授权工具只检查配置、终点、唯一性和 SHA256（安全哈希），不读取性能。
- 训练代码、任务 ID、地形分布、训练/holdout 地形 seed、B_ref、B80、固定权重 W100、评估初始状态、4 批次设计、指标和统计规则均不改变。
- 每次新增模型授权必须写入新的 JSON 文件，不得覆盖旧授权；每份评估结果记录对应授权文件的 SHA256。
- 渐进结果可以生成和检查运行完整性，但在 seed1–5 的配对结果齐备前不得形成正式跨方法结论，也不得据此修改后续训练、评估或筛选模型。
- 原始冻结器和评估器保持字节不变。渐进入口固定校验其 SHA256，只放宽授权数量门禁，以便保持训练时记录的仿真与指标实现哈希一致。
- 原机器冻结配置的状态文字仍为“B_ref 待标定、正式训练评估未开始”，已落后于实际进度；渐进授权不覆盖该旧文件，而是以其协议版本、边界审计版本及当前已冻结 B_ref 文件共同作为证据，并记录两份文件的 SHA256。

## 当前授权范围

首次渐进授权预计包含当前已经生成最终 `model_2999.pt` 的 15 个模型：seed1 的 14 个模型，以及 seed2 的 Flat / 任务型 PPO。正在 GPU1 续训的 seed1 Slope / 固定权重不在首次授权中，完成后必须进入新的授权文件再评估。

## 执行记录

- 首次授权于 2026-08-17 17:34 生成，包含 15 个模型；GPU2、GPU4、GPU5 完成全部 15 份评估。
- seed1 Slope / 固定权重完成后，于 2026-08-17 21:28 生成第二份不可覆盖授权，包含 16 个模型；GPU2 完成该模型评估。
- 截至 2026-08-17 21:33，seed1 五地形 × 三方法的 15 个模型以及 seed2 Flat / 任务型 PPO 均各自生成 4 批 × 50 回合的逐回合 JSON/CSV。
