# PACE-ECO 多地形“仅改变地形”协议修订

> 历史说明：本文记录v1.2→v1.3。现行v1.4继续保持“仅改变地形”，但因v1.3成功回合
> 真实足端跨块，改用65×60 m宽场；见`gpt-多地形宽场协议修订.md`。

日期：2026-08-06
协议：`gpt-multi-terrain-v1.2` → `gpt-multi-terrain-v1.3`

## 修订原因

v1.2 把历史 Flat 的固定20秒任务改成15 m固定距离走廊，并增加完成终止、侧向/后退
终止、完成奖励、2秒评估预热、性能课程和按地形归一化 PACE 约束。即使 PPO seed 相同，
它也不再是“只改变地形”的受控实验。

nullptr 明确要求主实验只改变地形。因此 v1.3 删除上述任务和算法变化，恢复历史 Flat
语义；长地形仅作为防止机器人跨入相邻地形块的空间容器。

## v1.3 删除的机制

- 15 m 完成线和提前完成终止；
- 侧向2 m、后退1 m终止；
- 完成奖励50；
- 按完成/失败升降难度的性能 curriculum（课程学习）；
- 新的多地形初始状态数值和2秒预热；
- `terrain_reference` 按地形归一化 PACE 约束；
- 固定15 m单位路线能耗作为任务指标。

## v1.3 恢复的历史语义

- 固定20秒，原奖励和仅 `time_out/base_contact` 终止；
- 5秒预热后的平均前进速度 `[0.8,1.2] m/s` 成功判据；
- 历史 calibration/holdout 初始状态数值；
- `absolute_j` 绝对焦耳 PACE-ECO 预算；
- 原 actor/critic 观测、动作、网络、摩擦、动力学、环境数和训练长度。

## 唯一新增的地形机制

- `plane` 改为40×10 m生成式地形；
- 五类地形、方向、几何实例和五层固定难度分布；
- 训练重置从所有难度层采样，但不按策略表现升降级；
- 只读记录类别、方向、难度、位移、路径和越界；这些记录不进入奖励、终止或 actor 观测；
- 前30/后2/侧3 m机身线只用于路线偏移预警；2026-08-07 首次 calibration 证明把它用作
  硬拒绝会误判仍在10 m宽地形块内的回合。
- 经 nullptr 确认，硬拒绝改为“接触足端中心越过真实地形块边缘”；摆动足越界仅预警。
- 边界审计 v2 使用1.25 m ANYmal D运动学上界做安全预筛：机身远离真实边缘时证明四足
  不可能越界，进入邻边带后才直接读取四足刚体中心；预筛不改变硬拒绝定义。
  该修订没有改变奖励、观测、动作、终止、地形几何或权重更新，发生在任何有效
  calibration JSON/CSV 写出和 B_ref 冻结之前。
- 2026-08-10 边界审计升为 v3：v2 flat calibration 有2个回合在第1000步前已因
  `base_contact` 失败，其中首个回合的接触足越过±5 m真实边缘。v3保留已失败
  的越界回合、继续计入成功率分母并单独报告；只有按历史定义原成功的回合发生
  接触足越界时，才因其成功指标可能受相邻几何污染而拒绝整份数据。此修订发生在
  B_ref 和正式 seed1--5 之前；v2 rough 结果归档但不用于冻结，五类必须统一重跑 v3。

## 保留但不属于“新调优”的选择

- PPO seed0继续标定 B_ref，seed1–5继续正式配对；
- 固定权重继续直接采用 PACE 论文 ANYmal `W100=-0.00016`；
- 阶段一每地形独立绝对焦耳B80；阶段二使用一个预注册的混合分布绝对焦耳B80；
- 最优固定权重搜索、条件化预算和多对偶变量推迟到独立新协议。

## 证据边界

v1/v1.2 GPU 冒烟和状态审计文件保留但已归档，不能放行 v1.3。v1.3 当前只有 CPU
静态几何与单元测试证据；实际出生穿透、碰撞网格方向、Isaac 终止项和完整仿真链路，
必须在 nullptr 确认后重新进行 GPU 冒烟和状态审计。
