# PACE-ECO 多地形论文固定权重协议修订

> 适用更新：PACE 论文 `W100=-0.00016` 在 v1.3 继续有效；本文的 v1.2 走廊、归一化
> PACE 预算和旧时间估算描述已被 v1.3 冻结配置取代。

> 历史说明：本文记录 v1→v1.1 的固定权重决策。当前 v1.2 继续完整沿用 W100=-0.00016；
> v1.2 仅修订 PPO seed 角色、训练数量和输出根，详见 `gpt-多地形seed复用协议修订.md`。

修订日期：2026-08-05 CST

协议变更：`gpt-multi-terrain-v1` → `gpt-multi-terrain-v1.1`

## 1. 决定

根据 nullptr 的决定，多地形主实验暂不搜索固定权重最优系数。固定权重 PPO 在阶段一
五类专用地形和阶段二五类混合地形中统一使用 PACE 论文为 ANYmal 给出的能耗奖励系数：

$$
c_e=-16\times10^{-5}=-1.6\times10^{-4}.
$$

归档中的 [gpt-计划.md](../archive/PACE-ECO-multi-terrain-legacy/gpt-计划.md)、
[claude-思路.md](../archive/PACE-ECO-multi-terrain-legacy/claude-思路.md)、平地环境配置和
原多地形固定权重配置对该数值的记录一致；沿用标签 W100。

## 2. v1.1 的解释边界

- 正式比较对象是“PACE 论文系数固定权重 PPO”，不是“每地形调优后固定权重 PPO”。
- 五类地形不分别改系数，混合地形也不另选全局系数。
- calibration 只用于任务型 PPO 标定各地形 `B_ref,t` 和 `B80,t`。
- 正式或 holdout 结果不得用于修改 W100。
- 后续若有时间调优，必须建立新协议版本、新 PPO/地形 seed、新 calibration/holdout
  实例和新输出目录；新结果作为扩展实验报告，不能反向替换 v1.1 主表。

## 3. 实现与证据变化

- 训练入口删除活跃 `fixed_grid_train` 角色；固定权重命令拒绝外部系数和选择 JSON。
- 评估入口禁止固定权重 calibration，并检查训练配置中的权重必须恰为 -0.00016。
- holdout 授权不再依赖固定权重选择文件，改为直接核验每个固定权重模型的环境配置。
- 已停用的网格选择工具会明确报错，避免误把 v1.1 结果用于事后选权重。
- 正式训练和评估输出根改为 `multi_terrain_v1_1`；历史 v1 文件不改名、不覆盖。
- 地形几何、路线、课程、任务 ID、actor/critic 观测、机器人动力学、B_ref 规则和正式
  seed 均未改变，因此历史五类 GPU 冒烟与碰撞状态审计只作为这些未变项的证据沿用。

## 4. 工作量变化

阶段一由 165 次训练降为 90 次：15 次 B_ref 训练和 75 次正式训练。阶段二由 33 次
训练降为 18 次：3 次 B_ref 训练和 15 次正式训练。两阶段合计 108 次训练、108 次评估。

按原 198 次方案的容量估计等比例缩放，并保留生成式地形碰撞开销余量，暂估总量为
340–880 GPU·小时。该数字是规划估计，不是长训练实测结果。

## 5. 当前停止点

本修订只修改代码、文档和非 GPU 校验对象，不授权或启动 3000 次正式训练、calibration
或 holdout。工作树经 nullptr 审阅并处理为干净状态、GPU 资源确认安全且获得正式训练
确认后，才从阶段一任务型 PPO 的 flat/seed100 B_ref 训练开始。
