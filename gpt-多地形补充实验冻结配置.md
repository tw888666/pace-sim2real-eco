# PACE-ECO 多地形补充实验冻结配置

协议版本：`gpt-multi-terrain-v1.4`

机器可读真值为 `gpt-多地形补充实验冻结配置.json`。若本解释文件与 JSON 不一致，必须
停止并修正，不能自行选择有利口径。

## 当前状态

- 唯一实验变量冻结为地形生成分布。
- v1.3 的40×10 m任务、五个seed0权重、rough calibration候选和GPU审计全部归档，
  不能放行或输入v1.4。
- v1.4代码、文档、68项非GPU测试、五类GPU冒烟、五类实际碰撞状态审计和rough的
  4096环境容量冒烟均已通过；正式训练及正式评估尚未开始。
- B_ref、B80和holdout尚未启封。

## 历史 Flat 不变量

固定20 s、1 m/s、5 s评估预热、原奖励、仅`time_out/base_contact`终止、原成功定义、
actor/critic观察、动作、网络、物理参数、摩擦、重置、4096环境和3000次更新全部继承。
PACE-ECO保持`absolute_j`约束；不设置完成距离、完成奖励或方向终止。

## 宽场和边界

| 项目 | 冻结值 |
|---|---:|
| 块长×块宽 | 65 m×60 m |
| 出生origin | 局部(30,30) m |
| 有效随机几何长度 | 29 m |
| 有效几何末端 | 局部x=59 m |
| 有效末端至前缘平台 | 6 m |
| 路线预警前/后/侧 | 30/2/3 m |
| 真实边缘（相对origin） | 后−30/前+35/左右±30 m |
| 足端相对机身冻结上界 | 1.25 m |
| 邻边带机身阈值 | 后−28.75/前+33.75/左右±28.75 m |
| 硬拒绝 | 任一回合的接触足中心越过真实边缘 |

机身预警和摆动足越界只记录。训练和评估都不新增边界终止。失败回合接触足越界也使整份
评估无效；这是正式数据资格审计，不是成功定义。v1.3 flat成功回合足端侧移6.440872 m是
扩大宽度的触发证据。

## 任务、采样和评估批次

- 每类三任务后缀统一为`Terrain20sWide-Anymal-D-v0`；历史Flat任务ID不变。
- 训练网格为5×10=50实例，难度`[0,1]`五层均匀重置，无表现驱动课程。
- 评估每批5×10=50实例，共4批；难度`[0.1,0.9]`。
- 第i批地形seed为拆分基准seed+i，i=0–3；四批合并后200个实例编号唯一。
- 八个历史calibration/holdout状态数值不变，使用Wide独立名称；全局各25回合。
- 只有四批齐全、编号完整、状态等量且接触足零越界时写正式JSON/CSV。

## seed、预算和固定权重

- PPO seed0标定；seed1–5正式配对；seed900/901仅冒烟。
- v1.4训练/calibration/holdout地形seed段见机器可读JSON，彼此且与v1.3不重叠。
- 阶段一每地形`B80,t=0.8×B_ref,t`；阶段二五类等权宏平均后使用单一
  `B80,mixed=0.8×B_ref,mixed`。
- 固定权重为PACE论文ANYmal `W100=-0.00016`；v1.4禁止本地网格选择。

## 输出与不可复用

- 模型/日志：`logs/supplementary/multi_terrain_v1_4/`
- 结果：`results/supplementary/multi_terrain_v1_4/`
- v1.3文件保留原位作为协议失败证据，不删除、不移动、不覆盖，也不作为v1.4输入。
- v1.4正式评估要求训练和评估实现源码精确一致，不再使用旧权重兼容哈希豁免。
