# PACE-ECO 多地形四足真实边界审计修订

> 已归档：本文记录v1.3的40×10 m边界审计v3。现行v1.4使用65×60 m宽场和边界审计v4，
> 任一失败或成功回合的接触足越界都会否决整份评估；见`gpt-多地形宽场协议修订.md`。

日期：2026-08-10
主协议：`gpt-multi-terrain-v1.3`
边界审计版本：`gpt-四足真实地形边界-v3`

## 修订原因与证据

五类任务型 PPO seed0 训练完成后，旧评估把机身侧向位移超过 `±3 m` 直接视为跨块。
rough calibration 的诊断运行中有9/200回合触发，最大机身侧移为
`4.033290863 m`；全部触发均来自侧向预警，前后向没有触发，机身中心也没有达到实际
地形块侧缘 `±5 m`。旧评估因此没有写出正式 JSON/CSV，B_ref 尚未冻结。

`±3 m` 是为10 m宽地形保留2 m余量的保守机身代理，但它不能回答四只脚是否实际接触
相邻地形。nullptr 确认改用四足直接审计。旧诊断日志保留，不删除或改写。

v2 rough calibration 随后完成200/200且无真实足端越界。v2 flat calibration 有2个
回合在第1000步前因 `base_contact` 已失败，其中首个失败回合的接触足最大侧向位移
`5.841049194 m`，越过真实边缘。v2的“任一回合越界即否决全部数据”会把必须保留在
成功率分母中的失败回合变成整份缺失。nullptr 因此确认升级为 v3。此时 B_ref 尚未冻结，
正式 seed1--5 尚未开始；v2 rough 不再用于冻结，五类统一重跑 v3。

## 冻结判据

- 足序固定为 `LF_FOOT、RF_FOOT、LH_FOOT、RH_FOOT`，启动时同时校验机器人和接触
  传感器解析出的名称与顺序。
- 足端位置定义为足端刚体中心的世界坐标，并转换为相对当前环境出生 origin 的坐标。
- 接触定义为该足净法向接触力范数严格大于现有 ContactSensor（接触传感器）的 `1 N`
  阈值；不增加或修改传感器配置。
- 40×10 m地形相对出生 origin 的真实平面边缘为：后向 `-5 m`、前向 `35 m`、左右
  `±5 m`。
- 失败越界：按历史定义已失败的回合即使接触足最小有符号边缘余量小于0，也保留回合、
  保持失败、计入成功率分母并单独报告。
- 硬拒绝：任一按历史定义原成功的回合发生上述接触足真实越界。
- 仅预警：摆动足端中心越过真实块边缘；机身超过前30、后2或侧3 m路线预警线。
- 每回合保存四足各自的接触越界和摆动越界。进入边缘邻近审计带的回合还保存该审计带
  内接触时最小边缘余量、最大/最小前向位置和最大侧向绝对位置；这些位置统计不得误写成
  全回合极值。

### 冻结的安全预筛

逐控制步读取全部200个环境的足端 link transform（连杆变换）会把约3分钟的评估拖慢到
60分钟以上，但预筛不能退回“机身越过±3 m就失败”的旧代理判据。

ANYbotics 官方 ANYmal D 简化 URDF 给出的 base 到 FOOT 刚体中心各段固定平移为：

- base 到 HAA：`(0.304, 0.109, 0) m`；
- hip 到 HFE：`(0.069, 0.006, 0) m`；
- thigh 到 KFE：`(0, 0.1805, -0.285) m`；
- shank 到 FOOT：`(0.1, 0.02225, -0.39246) m`。

各段长度之和为 `1.1351 m`。关节旋转不改变段长，因此三角不等式给出与关节角无关的
足端中心—机身中心距离上界。协议冻结更保守的 `1.25 m`：

- 机身到最近真实块边缘的余量严格大于 `1.25 m` 时，数学上可证明四个足端中心不可能
  越界，不读取足端变换；
- 机身边缘余量小于等于 `1.25 m` 时，直接读取四足刚体中心和接触力；真实越界始终记录，
  最终是否硬拒绝再由回合的历史成功状态决定；
- 对40×10 m块和出生 origin_x=5 m，邻边带阈值为后向 `-3.75 m`、前向 `33.75 m`、
  侧向 `±3.75 m`；原前30、后2、侧3 m只保留为报告预警，不参与硬拒绝。

URDF 来源：
`https://github.com/ANYbotics/anymal_d_simple_description/blob/master/urdf/anymal.urdf`。

当前 Isaac Lab 接触传感器没有在本任务中启用逐接触点位置输出，因此主判据使用足端刚体
中心而不是鞋底接触斑的每个几何点。该限制必须在结果中说明；如果未来启用逐接触点审计，
需要新审计版本，不能静默替换。

v1 CPU 几何审计覆盖63组实例并全部通过，证据为
`gpt-多地形四足边界几何静态审计.json`。v2 另用单元测试锁定 `1.1351 < 1.25 m`、
邻边带临界点和中心安全区。GPU0 rough 仿真状态审计验证接触传感器视图、关节体物理视图
和原 `body_pos_w` 的足端中心坐标最大绝对差均为 `0.0 m`，并检查首步无接触足越界；
证据位于 `logs/supplementary/multi_terrain_v1_3/state_audit/` 的对应时间戳日志和 JSON。

## 权重与复现兼容性

本修订只改变评估数据的有效性与报告判据，不进入奖励、actor/critic观测、
动作、终止、地形生成或 PPO 更新。已经完成的五个 seed0 模型无需重训。

正式评估仍要求干净且已提交的工作树。加载修订前权重时，校验器只允许以下七个源码差异：

- `pace_eco_lab/envs/terrain20s_env.py`；
- `pace_eco_lab/multi_terrain_protocol.py`；
- `pace_eco_lab/multi_terrain_statistics.py`；
- 新增 `pace_eco_lab/terrain_boundary.py`；
- `scripts/pace_eco/freeze_multi_terrain_budgets.py`；
- `scripts/pace_eco/freeze_multi_terrain_holdout.py`；
- `scripts/pace_eco/multi_terrain_eval.py`。

其他源码哈希必须与训练记录完全一致，评估摘要同时保存训练与评估 commit（提交）及差异
文件。未来模型若使用修订后源码训练，则继续要求训练与评估源码完全一致。
