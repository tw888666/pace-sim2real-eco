# PACE-ECO 多地形 GPU 冒烟验证报告

> 已归档：本报告验证的是 v1/v1.2 的 24×6 m、15 m 走廊实现，不能作为 v1.3
> Terrain20s 的放行证据。本报告撰写时 v1.3 尚未执行；当前 v1.3 已通过，见
> `gpt-多地形Terrain20s-GPU冒烟验证报告.md`。

协议版本：`gpt-multi-terrain-v1`

执行时间：2026-08-05 09:48–10:02 CST

## 1. 执行边界与结论

在 nullptr 明确授权后，仅执行阶段一五类地形的任务型 PPO（近端策略优化）链路冒烟：
每类 16 个环境、2 次更新、PPO seed 900。没有启动 calibration（校准）、holdout
（留出集）评估、B_ref 标定或 3000 次正式训练，也没有生成正式评估 JSON/CSV。

最终 flat、rough、stairs、boxes、slope 五类均以退出码 0 完成 2/2 次更新。每类均生成
`model_0.pt` 和 `model_1.pt`；这些权重位于隔离的 `smoke` 目录，按脚本安全门禁止用于
calibration、holdout 或正式统计。actor（策略网络）实际输入为 48 维纯本体观测，
critic（价值网络）实际输入为 353 维，其中包括 294 维特权高度扫描。

## 2. GPU 选择与已有任务保护

执行前 GPU2 已占用约 29.3/32.8 GiB，未使用。最初选择 GPU4，但其上有其他用户的
长期训练 PID 1630127；首次 flat 暴露环境构造期 CUDA（并行计算平台）同步卡点后，改用
GPU0 串行执行。GPU0 当时约有 25.7 GiB 显存余量且计算利用率为 0；已有 ComfyUI
（图形工作流）进程未被停止或修改。GPU5 未使用。

收尾时 GPU0 回到执行前的 56158 MiB 占用，未发现本工作树残留的训练或评估进程。
原 PACE-ECO 平地日志、模型和评估结果均未修改。

## 3. 冒烟中发现并修复的问题

初版在 `PaceCorridorRLEnv.__init__` 末尾通过 `torch.any` 同步读取 CUDA 上的出生状态。
实际运行显示，Isaac Sim（机器人仿真平台）刚完成环境初始化、尚未推进首个物理步时，
该同步会长时间阻塞，策略网络和训练日志都无法创建。阶段定位信息在 GPU4 和 GPU0 上
复现了同一位置，因此不能把问题仅归因于某张 GPU 的负载。

修复后，环境构造期仍硬检查地形 origin 网格、列数和每环境地形索引形状；出生点横向
偏移、机身高度和索引取值范围改为在首个真实物理步完成后同步硬检查。五个成功任务均
打印“首个物理步后的出生点与地形索引运行时校验通过”。这保留了安全门，同时避免在
Isaac Sim 初始化与 CUDA 互操作尚未进入稳定步进阶段时强制同步。

为定位问题保留了 3 个未完成 flat 目录：GPU4 两次、GPU0 一次。它们均未生成模型，
没有 Traceback（异常堆栈）、CUDA error、out of memory（显存不足）、NaN 或 Inf；进程
由本次执行者优雅中断，未删除目录或日志。

## 4. 五类成功结果

| 地形 | 地形 seed | 成功运行目录时间戳 | 更新 | 最终检查点 | 首步运行时校验 | 单次更新时间 |
|---|---:|---|---:|---|---|---|
| flat | 910900 | `2026-08-05_10-00-14` | 2/2 | `model_1.pt` | 通过 | 2.45 s、2.03 s |
| rough | 911900 | `2026-08-05_10-00-40` | 2/2 | `model_1.pt` | 通过 | 1.68 s、1.93 s |
| stairs | 912900 | `2026-08-05_10-01-05` | 2/2 | `model_1.pt` | 通过 | 1.97 s、2.06 s |
| boxes | 913900 | `2026-08-05_10-01-26` | 2/2 | `model_1.pt` | 通过 | 1.77 s、1.82 s |
| slope | 914900 | `2026-08-05_10-02-14` | 2/2 | `model_1.pt` | 通过 | 1.70 s、1.88 s |

每类共完成 768 个环境步。rough、stairs、boxes、slope 使用独立生成种子；stairs 和
slope 的任务配置均包含上行、下行列。boxes 的 terrain generation（地形生成）约
0.24 秒，但 scene creation（场景创建）约 27.85 秒，明显高于其余类别约 2–4 秒；这是
正式容量规划需要保留的实测开销，但本次没有发生显存异常或运行失败。

成功运行根目录：

`logs/supplementary/multi_terrain_v1/smoke/rsl_rl/pace_task_only_<terrain>_corridor_anymal_d/`

启动日志根目录：

`logs/supplementary/multi_terrain_v1/smoke/launch/`

## 5. 验证与仍未关闭的门

- 修改后限定单元测试：50 项全部通过。
- `git diff --check`：通过。
- 五个成功日志未检出 Traceback、CUDA error、out of memory、NaN、Inf 或出生穿透错误。
- Isaac Sim 会因 `CUDA_VISIBLE_DEVICES` 的设备重映射打印“Skipping GPU due CUDA being in
  bad state”警告，但设备表明确显示所选物理 GPU0 为 active，五次训练均正常完成；正式
  运行仍应逐次核对 active 设备和退出码，不能只忽略该警告。
- 本次 2 次更新只验证初始化、首步动力学、观测、奖励、终止、课程、日志和 PPO 更新链路。
  它不能证明未训练策略可走完整条 15 m 路线，也不能代替方向/完成线的长距离仿真状态审阅。
- 本报告生成后，完整路线状态核验已另行通过，见 `gpt-多地形仿真状态审计报告.md`；
  阶段二 mixed 冒烟仍未执行，且不得未经新授权开始 B_ref 或任何正式训练。

## 6. 停止点

本报告当时停止在“五类阶段一任务型 GPU 链路冒烟通过”。后续状态审计已完成；当前等待
nullptr 审阅并自行提交 Git（版本控制系统）冻结实现，不自动启动正式训练或评估。
