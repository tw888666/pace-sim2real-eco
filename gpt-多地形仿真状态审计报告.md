# PACE-ECO 多地形仿真状态审计报告

> 已归档：本报告审计的是 v1/v1.2 完成线、越界终止和课程实现；这些机制已从 v1.3
> 主协议移除。本报告撰写时 v1.3 尚未执行；当前 v1.3 已通过，见
> `gpt-多地形Terrain20s仿真状态审计报告.md`。

协议版本：`gpt-multi-terrain-v1`

执行时间：2026-08-05 11:31–11:32 CST

## 1. 结论与执行边界

在 nullptr 授权继续 GPU 命令后，使用 GPU0 对阶段一 flat、rough、stairs、boxes、slope
逐类完成实际 Isaac Sim（机器人仿真平台）状态审计，五类均为“全部通过”。每类生成
5 个课程等级 × 20 个地形列，共 100 个实际碰撞地形实例；共检查 500 个实例。每个实例
使用实际 `/World/ground` 碰撞网格射线覆盖局部 x=-2–20 m、y=-1.8–1.8 m。

本审计不加载、不训练、不生成或修改模型权重，也不是 calibration（校准）或 holdout
（留出集）正式评估。只新增隔离的状态审计日志和 JSON（结构化结果），未修改原平地
模型、日志或评估结果。

## 2. 通过项

每类 19 项总检查全部通过，包括：

- 首个真实物理步无意外终止，出生点与运行时地形行列索引合法；
- 实际碰撞网格覆盖完整 15 m 路线及完成线后 6 m 安全平台；
- 起点平台、origin 高度和完成平台有限且符合协议；
- 15 m 完成线对全部课程等级/列触发，且记录完成进度、等级和列号；
- 后退 -1 m 越界对全部等级/列触发，且记录越界、等级和列号；
- 成功后非最高等级升一级，最高等级回绕到有效等级，失败降一级；
- 课程五行分别落入 [0,0.2)、[0.2,0.4)、[0.4,0.6)、[0.6,0.8)、[0.8,1.0)，同列严格变难；
- 横向审计范围小于 ±2 m 终止线和 ±3 m 块边界，未进入相邻地形块；
- 楼梯、斜坡分别有 50 个上行和 50 个下行实例，并通过方向符号和有效段单调检查；
- 粗糙高度、箱块高度和箱块逐纵向采样通道均在冻结范围内；斜坡终点后不再延伸，
  排除了无限斜坡。

实际中心线路线高度范围如下：

| 地形 | 地形 seed | 方向实例 | 路线最低相对高度 m | 路线最高相对高度 m | 净高差范围 m |
|---|---:|---|---:|---:|---:|
| flat | 910900 | level 100 | -0.000004 | 0.000002 | 0 |
| rough | 911900 | level 100 | -0.056818 | 0.056980 | 约 0 |
| stairs | 912900 | up 50 / down 50 | -2.309319 | 2.206095 | -2.309317 至 2.206095 |
| boxes | 913900 | level 100 | -0.000004 | 0.158489 | 0 |
| slope | 914900 | up 50 / down 50 | -2.756347 | 2.753008 | -2.756346 至 2.753008 |

## 3. 证据文件

结果根目录：

`logs/supplementary/multi_terrain_v1/state_audit/results/`

| 地形 | 结果文件 | SHA256（安全哈希） |
|---|---|---|
| flat | `gpt-flat-仿真状态审计-20260805_113109.json` | `0330d3740ebd25d9c669c184e31cb7ac8b011c296d7486209ea56d05a0e638cc` |
| rough | `gpt-rough-仿真状态审计-20260805_113128.json` | `03e01a7adb1022be6648af78003013c23ddf89086e40d0d65477ddb66ad90a94` |
| stairs | `gpt-stairs-仿真状态审计-20260805_113143.json` | `778d080ddaf43e5152c4a5b5d64707cf850359fe3bd5f5a276014fca0c5c76d3` |
| boxes | `gpt-boxes-仿真状态审计-20260805_113155.json` | `a00aab46dd6f89a932330cd5b4ce3534011db5aeb2110e92b78307d0f073e321` |
| slope | `gpt-slope-仿真状态审计-20260805_113208.json` | `4e716adb91aacc4c79d3ee1e7c481113c180bc37882da3c747ac4423621d7a6a` |

启动日志位于 `logs/supplementary/multi_terrain_v1/state_audit/launch/`。最终五份日志均正常
结束。定位脚本期间保留了 6 份较早的 flat 诊断日志；它们没有结果 JSON，也不属于通过
证据。诊断确定原因为审计入口未显式重置环境，以及 float32（32 位浮点数）中心线舍入
不应按严格零判断；对应实现已修复并增加回归测试。

## 4. GPU 与停止点

审计开始前 GPU0 占用 56158 MiB，结束后回到 56158 MiB；未残留本审计进程。GPU5
未使用，其他用户进程未停止或修改。

后续正式训练前审查完成后，收尾验证增至 58 项非 GPU 测试全部通过；新增/修改 Python 文件编译通过，五个多地形 shell
（命令解释器）入口语法检查通过，`git diff --check` 通过。

阶段一的静态几何门、五类任务型 PPO（近端策略优化）2 次更新链路冒烟门和实际仿真
状态门现已关闭。下一步是 nullptr 审阅脏工作树并自行决定是否 commit（提交）；3000 次
正式 B_ref 训练仍未授权，且脚本会拒绝在脏工作树或普通 SSH 前台启动。
