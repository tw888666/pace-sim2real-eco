# PACE-ECO 多地形补充实验只读检查报告

> 历史快照：保留首次只读检查的时间点状态。它不代表 2026-08-06 的实时 GPU、tmux、
> 进程或 v1.3 实现状态；正式启动前必须重新只读检查。

检查时间：2026-08-04 17:03–17:10 CST

检查对象：原工作树 `/home/xy.chen/tw/PACE-ECO`。本报告只记录启动实施前的只读快照。

## 1. Git 与运行环境

- 原工作树位于 Git（版本控制系统）branch（分支）`exp/fixed-weight-grid-v2`，检查时
  HEAD 为 `6c072b68b08c2f7875bdffa798d7ca882f06dde2`。
- 原工作树存在多项固定权重网格相关的已修改和未跟踪文件，不能安全地 stash（暂存）、
  commit（提交）或切换分支。
- 因此，多地形实现使用独立 Git worktree（独立工作树）
  `/home/xy.chen/tw/PACE-ECO-multi-terrain`，其分支为
  `exp/multi-terrain-comparison-v1`。原工作树和已有结果没有被移动或覆盖。
- GPU（图形处理器）快照：GPU0 有其他用户约 14.4 GiB 占用；GPU1、GPU2 当时空闲；
  GPU3 有其他进程且利用率约 98%；GPU4 有其他进程约 7.7 GiB 占用；GPU5 按项目规则
  禁止使用。该信息只代表检查时刻，正式启动前必须重新检查。
- `tmux list-sessions` 显示多个历史 shell 和 TensorBoard（训练指标可视化工具）会话；
  未发现正在运行的 PACE 训练或正式评估进程。只发现 PACE TensorBoard 进程，未停止。

## 2. 平地正式实验

- `calibration_v1`：任务型 PPO、固定权重 PPO、PACE-ECO 各 seed 1–5，共 15 份摘要和
  15 份逐回合 CSV，均存在。
- `holdout_v1`：三种方法各 seed 1–5，共 15 份摘要和 15 份逐回合 CSV，均存在。
- 三种方法正式终点 `model_2999.pt` 共 15 个均存在；额外的 seed 0 文件属于历史先导，
  不能计入正式样本。
- 冻结平地预算仍为 `B80 = 17015.6609 J`，只适用于平地、1 m/s、20 s 旧协议。
- 已有正式汇总记录的联合合格率依次为：任务型 PPO 0%、固定权重 PPO 19.6%、
  PACE-ECO 80.7%；成功率依次约为 99.6%、99.6%、100%。本次多地形实现不改写这些结果。

## 3. 平地固定权重补充实验

- 固定权重网格 seed 6–8：15 个 `model_2999.pt` 与 15 份 `calibration_v2` JSON/CSV
  均存在。
- 冻结选择结果为 `W400 = -0.00064`；选择集内三个 seed 的联合合格率均为 100%。
- 最终配对训练 seed 9–13：固定权重和 PACE-ECO 共 10 个 `model_2999.pt` 均已完成；
  最后一个 seed 13 于 2026-08-04 16:57 左右结束。
- 检查时尚无 `holdout_v2` 结果目录，因此“训练已完成”，但整个补充实验尚未完成最终
  留出评估和统计。多地形工作不得干扰这一步。

## 4. 生成式地形基础

当前代码已经具备以下基础：

- `TerrainImporterCfg` 地形导入配置和地面碰撞材质；
- actor（策略网络）48 维纯本体观测；
- critic（价值网络）353 维特权观测，其中保留 294 点高度扫描；
- 接触传感器、确定性评估状态、任务注册和逐物理子步能耗积分；
- 三种方法共用的网络、动力学参数、摩擦和 1 m/s 指令。

当前正式环境仍为 `terrain_type="plane"`，课程为空；没有生成器、固定距离完成条件、
跨块防护、地形 seed 隔离或方向/难度记录。因此已有基础足以扩展，但绝不能只把
`plane` 改成 `generator` 后直接训练。

## 5. 检查结论

原平地结果完整且保持只读；平地固定权重最终训练已结束，但 `holdout_v2` 尚未完成。
多地形功能只能在独立工作树和独立输出根中继续。任何 GPU 冒烟、正式训练或正式评估
都必须等待 nullptr 确认。

## 6. 完成实施后的只读复查

2026-08-04 17:44 CST 再次检查原工作树：仍位于 `exp/fixed-weight-grid-v2` 和同一 HEAD，
原有未提交修改仍在；没有 PACE 训练或评估进程，只有原 TensorBoard。固定权重补充目录
仍为 25 个 `model_2999.pt`（15 个网格 + 10 个正式），`holdout_v2` JSON/CSV 仍为 0。

GPU 状态已经相对 17:03 快照变化：GPU0 约 14.4 GiB、GPU1 约 17.8 GiB、GPU3 约
0.8 GiB 且高利用率、GPU4 约 7.7 GiB 且高利用率均为其他任务；GPU2 当时空闲；GPU5
仍禁止使用。该变化说明后续命令不能把某张卡长期写死为“空闲”，每次启动前都要重新
执行 `nvidia-smi`。
