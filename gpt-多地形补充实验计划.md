# PACE-ECO 多地形补充实验计划

协议版本：`gpt-multi-terrain-v1.4`（Terrain20sWide，仅改变地形）

状态：代码、文档和非 GPU 校验进行中；v1.4 GPU（图形处理器）冒烟、仿真状态审计、
3000 次训练和正式评估均未开始。

## 1. 唯一实验变量

三种方法为任务型 PPO（近端策略优化）、PACE 论文固定权重 PPO
`W100=-0.00016` 和 PACE-ECO。历史 Flat 的固定 1 m/s 指令、20 s 回合、奖励、
`time_out/base_contact` 终止、5 s 预热后的成功定义、actor/critic（策略/价值网络）观察、
动作、网络、4096 环境、3000 次更新、物理参数、摩擦、重置和 PACE-ECO `absolute_j`
（绝对焦耳）算法全部继承。主实验不增加终点、完成奖励、方向约束或高度扫描。

唯一变化是把 `plane` 替换成指定类别的生成式地形。固定距离和 J/m 目标若研究，必须另建
任务和协议；本协议的 J/m 只作描述指标。

## 2. Terrain20sWide 宽场设计

v1.3 的 40×10 m 块无法容纳历史成功定义允许的横向漂移：flat calibration 已观察到
成功回合接触足最大侧向位移 6.440872 m，足端进入相邻块。v1.4 因此使用 65×60 m 块，
出生 origin 为局部 `(30,30)` m：

- 相对出生点真实边缘为后 −30 m、前 +35 m、左右 ±30 m；
- 有效随机几何前向长度仍为 29 m，局部 `x=59 m` 结束，至前缘保留 6 m 同高度平台；
- 历史成功速度上限 1.2 m/s 在 20 s 的名义路程为 24 m；扣除 1.25 m 足端运动学上界后，
  后向和侧向仍有 4.75 m 余量；
- 前30、后2、侧3 m仍只作路线预警；不改变回合、奖励、终止和成功定义；
- 机身距真实边缘大于1.25 m时由ANYmal D运动学上界证明四足安全，进入邻边带才读取四足
  刚体中心；任一回合的接触足中心越过真实边缘，整份评估无效且不写正式结果；
- 摆动足越界只预警。训练和评估均不新增边界终止。

## 3. 阶段一：五类分别训练

每个专用任务生成5个难度层×10个几何列，共50个训练地形实例。生成器的
`curriculum=True` 只生成按行分层的几何；环境没有表现驱动升降级，重置从五层均匀采样。

| 类别 | 冻结随机范围 | 方向 |
|---|---|---|
| 平地 | 生成式有限平面，作为新实验内部控制 | level |
| 粗糙 | 起伏0.015–0.060 m，相关尺度0.45–0.90 m | level |
| 楼梯 | 台阶高0.030–0.080 m，名义宽0.75–1.10 m | 上、下各50% |
| 箱块 | 高0.040–0.160 m，随机位置、尺寸和间距，保留绕行通道 | level |
| 斜坡 | 3–8°有限坡，两端接平台 | 上、下各50% |

每类先训练任务型 seed0，在 calibration（校准集）标定 `B_ref,t`，冻结
`B80,t=0.8×B_ref,t`；再从头训练三方法×seed1–5。五类 v1.3 seed0 权重、rough 的
29074.5931 J 候选和所有旧评估均归档，不得输入 v1.4。

## 4. 阶段二：混合训练

阶段一协议和统计规则冻结后，从头训练三个通用模型系列，不拼接专用模型。混合列比例是
平地/粗糙/箱块各20%，上/下楼、上/下坡各10%。训练每行10列可精确分配为
2/2/2/1/1/1/1列。

PACE-ECO 预注册单一混合总预算：任务型混合 seed0 calibration 分别得到五类诊断
`B_ref,t`，五类等权宏平均为 `B_ref,mixed`，然后冻结
`B80,mixed=0.8×B_ref,mixed`，使用一个绝对预算和一个对偶变量。条件化预算和每地形独立
对偶变量不进入本主实验。

## 5. seed 和数据隔离

- PPO seed0：B_ref 训练；seed1–5：三方法正式配对；seed900/901：仅冒烟；
- v1.4 地形 seed 使用31万–45万正式段和93万/94万冒烟段，与 v1.3 分离；
- 同地形、同阶段、同PPO seed的三方法使用相同训练地形 seed；
- 训练、calibration 和 holdout（留出集）地形 seed 互不重叠；
- 每个评估数据集分4批，每批5×10=50个新几何，批次 seed 为基准 seed 加0、1、2、3；
- 四批全局实例编号为0–199，八个历史初始状态各出现25次；只在四批全部通过后写正式
  200回合JSON/CSV。

新任务后缀为 `Terrain20sWide-Anymal-D-v0`，训练和结果根分别为
`logs/supplementary/multi_terrain_v1_4`、`results/supplementary/multi_terrain_v1_4`。
因此不会覆盖历史 Flat、v1.3 权重或结果。

## 6. 评估与统计

阶段一每份200回合只含一个类别；阶段二每类40回合，上/下楼和上/下坡各20回合。主指标
为成功且能耗不超过主预算的联合合格率。次指标包括成功率、速度误差、总/电气/机械/势能、
净前进距离、实际路径、两种J/m、`E_t/B_ref,t`、协调性、方向、难度和seed间样本标准差。
跨地形必须先按seed和类别计算，再做五类等权宏平均并报告最差地形。

## 7. 数量与 GPU 时间

| 阶段 | B_ref训练 | 正式训练 | 训练合计 | calibration数据集 | holdout数据集 | 评估数据集合计 |
|---|---:|---:|---:|---:|---:|---:|
| 阶段一 | 5 | 75 | 80 | 5 | 75 | 80 |
| 阶段二 | 1 | 15 | 16 | 1 | 15 | 16 |
| 合计 | 6 | 90 | 96 | 6 | 90 | 96 |

每份评估需4次Isaac Sim启动，因此正式评估共有384个GPU进程批次，但仍是96份统计数据集。
v1.4最重rough已在GPU0以4096环境实测：地形生成7.58秒、场景创建136.38秒、仿真启动
48.20秒，两次更新时间6.03秒和4.14秒。在仅剩约22492 MiB可用显存时仍完成，未发生
显存不足。仅按这两个早期更新外推，单个rough任务型3000更新约3.5–5.1 GPU·小时；但
两次更新不足以代表稳定长训练，PACE-ECO开销、其他地形和评估批次也尚未实测。因此总体
容量规划仍保守使用每次3–8 GPU·小时、96次训练约288–768 GPU·小时；完成首个seed0
长训练及一次50更新计时后再收紧区间，不能把本次短冒烟当作正式时间承诺。

## 8. 冻结执行顺序

1. [已完成] 完成代码、机器可读配置、中文文档和非GPU测试；
2. [已完成] 在干净 commit（提交）后，经 nullptr 确认执行五类GPU冒烟；
3. [已完成] 执行五类实际碰撞网格状态审计和rough 4096环境容量冒烟；
4. [下一门] 经nullptr再次确认后，训练阶段一五个任务型seed0；
5. 四批calibration均通过后冻结五个`B_ref,t/B80,t`；
6. 从头训练阶段一75个正式模型，冻结授权后启封holdout并统计；
7. 阶段一规则和结论冻结后，再按同样门控执行阶段二。

任何GPU测试揭示宽场尺寸或批量不可用时，必须在B_ref和正式训练前保留证据、升级协议并
重新测试；不得静默缩小地形或复用v1.3结果。
