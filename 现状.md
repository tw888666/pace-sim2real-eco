# PACE + ECO 研究现状与后续计划

> 最后更新：2026-07-26  
> 当前分支：`codex/pace-eco-ppolag`  
> 当前开发基线：`0dab587`（包含此前代码和实验产物；P0 基础设施正在其后实现）

## 1. 一句话结论

PACE 能耗约束训练的基础链路已经跑通：参数辨识模型、400 Hz 能量累计、PPO-Lagrangian（近端策略优化—拉格朗日）算法、独立对偶评估、断点恢复和 GPU（图形处理器）训练都已验证可运行。

当前尚不能启动正式多种子实验。主要阻塞不是策略不会走，而是 cost critic（代价价值网络）未通过校准门槛。现已实现新版全轨迹指标、冻结 actor 的一次性轨迹数据集、零时间/真实时间/time-only 配对对照、49 维 cost 时间上下文和真正的 critic-only（仅代价价值网络）预热模式。下一步由用户在 A100 上采集一次冻结策略数据；在对照结果支持时间假设前，不继续联合训练或更新拉格朗日乘子。

## 2. 研究目标与边界

### 2.1 目标

保留 PACE 的两项核心能力：

1. 使用官方 `pace_data` 和 CMA-ES（协方差矩阵自适应进化策略）得到的执行器参数，建立辨识后的 ANYmal-D 仿真模型。
2. 使用 PACE 论文中的物理能耗模型计算电气功率、机械功率和势能功率。

在第三阶段训练中，将原来的固定能耗 reward（奖励）及其 curriculum（课程调度）替换为 ECO 风格的能量约束：

- reward：速度跟踪、碰撞、FTD 等任务目标；
- cost（代价）：PACE 物理能耗；
- 算法：PPO 改为 PPO-Lagrangian；
- 目标：在校准仿真中满足指定的模型能量预算，同时尽量保持运动性能。

### 2.2 研究边界

当前没有真实机器人，因此本项目定位为 simulation-only（纯仿真）研究：

- 可以使用官方真实机器人数据离线辨识执行器参数；
- 可以比较不同算法在辨识后仿真模型中的能耗和运动性能；
- 不验证真实电池能耗，不做真机策略部署；
- 不能声称方法保证真实机器人满足电池能量预算；
- 合理主张是：该方法在校准仿真中比固定能耗奖励更稳定地满足模型能量预算。

## 3. 已固定的软件、硬件与数据基线

| 项目 | 当前固定值 | 说明 |
| --- | --- | --- |
| 操作系统 | Ubuntu 22.04 Server | 无 GUI（图形用户界面），通过 SSH（安全外壳协议）连接 |
| Isaac Lab | 提交 `b4c321024792976150ca55fddb26fa34480d974e` | 当前验证基线 |
| RSL-RL | `rsl-rl-lib==5.0.1` | 与上述 Isaac Lab 提交一致 |
| PyTorch | `2.7.0+cu128` | CUDA（统一计算设备架构）构建版本 12.8 |
| 可用训练 GPU | GPU 0、1：A100 80 GB | 当前命令使用物理 GPU 0，并在进程中映射为 `cuda:0` |
| 不建议使用 | GPU 2～4：V100；GPU 5 | 当前 PyTorch 不支持 V100 的 `sm_70`；服务器约定 GPU 5 不可用 |
| 机器人 | ANYmal-D 仿真模型 | 12 自由度四足机器人 |
| PACE 代码基线 | 提交 `0703812` | 本项目改动前的本地基线 |
| 辨识参数 | `fitting.npy`，49 维 | SHA-256：`4436941fa5e9a5e8e1ef93d55956fcffdb4c4c4526b8ef3e145e6ea619fbe1c8` |

49 维辨识参数的顺序已经核对：12 个转子惯量、12 个黏性摩擦、12 个静/动摩擦、12 个编码器偏置、1 个延迟。第 49 维为 `3.2406`，PACE 实现向下取整为 3 个物理步；物理步长为 0.0025 秒，因此延迟为 7.5 毫秒。

编码器偏置约定为：

\[
q_{\mathrm{enc}}=q_{\mathrm{sim}}-b.
\]

若默认姿态属于编码器坐标系，则复位时使用 \(q_{\mathrm{sim}}=q_0+b\)。观察、复位和执行器中不能重复施加偏置。

## 4. 当前已实现的代码

### 4.1 物理能耗模型与 400 Hz 累计

相关代码：

- [`source/pace_sim2real/pace_sim2real/energy/model.py`](source/pace_sim2real/pace_sim2real/energy/model.py)
- [`source/pace_sim2real/pace_sim2real/tasks/manager_based/pace/pace_energy_env.py`](source/pace_sim2real/pace_sim2real/tasks/manager_based/pace/pace_energy_env.py)

PACE 功率模型为：

\[
P_{\mathrm{el}}=\sum_j 0.0192\tau_j^2,
\]

\[
P_{\mathrm{mech}}=
\begin{cases}
\tau^\top\dot q, & \tau^\top\dot q\ge 0,\\
0, & \tau^\top\dot q<0,
\end{cases}
\]

\[
P_{\mathrm{pot}}=\sum_b m_b g v_{b,z}.
\]

仿真以 400 Hz 运行，策略以 50 Hz 运行。每个策略步准确累计 8 个物理子步：

\[
E_k=\sum_{s=1}^{8}0.0025
\left(P_{\mathrm{el},k,s}+P_{\mathrm{mech},k,s}+P_{\mathrm{pot},k,s}\right).
\]

当前使用延迟和限幅后的 `applied_torque`，不重复加入 PhysX（物理引擎）的被动摩擦力矩；势能项使用刚体质心的世界系线速度。刚体质量张量只在初始化时复制到仿真设备，已修复 CPU/GPU 设备不一致问题。

### 4.2 PPO-Lagrangian

相关代码：

- [`source/pace_sim2real/pace_sim2real/algorithms/ppo_lagrangian.py`](source/pace_sim2real/pace_sim2real/algorithms/ppo_lagrangian.py)
- [`source/pace_sim2real/pace_sim2real/tasks/manager_based/pace/agents/rsl_rl_ppo_cfg.py`](source/pace_sim2real/pace_sim2real/tasks/manager_based/pace/agents/rsl_rl_ppo_cfg.py)

已经实现：

- 独立的 reward critic（奖励价值网络）和 cost critic；
- 独立 cost storage（代价轨迹存储）、优化器和 GAE（广义优势估计）；
- `gamma_cost=1.0`、`cost_gae_lambda=0.95`；
- 普通 rollout（采样段）边界允许 cost critic 自举；
- 摔倒终止或真实 20 秒 timeout（超时）时，cost 的 `next_value=0`；
- reward 与 cost 使用不同终止掩码；
- actor（策略网络）损失对 reward/cost 策略代理按 \(1+\lambda\) 归一化，熵和两个 critic loss 不做该归一化；
- 失败回合的增广代价下限为 1.25，但屏障值不计入物理焦耳数和回合能量违反比例。

### 4.3 独立 dual-eval 协议

相关脚本：

- [`scripts/pace/freeze_dual_checkpoint.py`](scripts/pace/freeze_dual_checkpoint.py)
- [`scripts/pace/dual_eval.py`](scripts/pace/dual_eval.py)
- [`scripts/pace/apply_dual_eval.py`](scripts/pace/apply_dual_eval.py)
- [`scripts/pace/check_cost_critic_gate.py`](scripts/pace/check_cost_critic_gate.py)

已经实现不可变 checkpoint（检查点）、SHA-256 校验、cycle ID（评估周期标识）、原子 JSON（JavaScript 对象表示法）提交和幂等的乘子更新。评估进程不会直接修改训练 checkpoint；同一评估结果重复应用时不会再次更新 \(\lambda\)。

正式设计为：每 \(K=100\) 次 PPO 更新冻结一次当前策略，在独立 256 个环境中跑完整 20 秒回合，再由编排步骤更新一次 \(\lambda\)。开发诊断阶段可以使用 \(K=50\)。

### 4.4 训练、恢复与兼容性

相关代码：

- [`scripts/rsl_rl/train.py`](scripts/rsl_rl/train.py)
- [`source/pace_sim2real/pace_sim2real/tasks/manager_based/pace/anymal_pace_eco_env_cfg.py`](source/pace_sim2real/pace_sim2real/tasks/manager_based/pace/anymal_pace_eco_env_cfg.py)

已经完成：

- 适配当前 Isaac Lab I/O API（输入/输出接口）；
- 适配 RSL-RL 5.0.1 的构造和配置接口；
- checkpoint 保存 actor、reward critic、cost critic、各自优化器和 \(\lambda\)；
- 新增 `--reset_cost_critic_on_resume`，可在保留 actor、reward critic 和主优化器的情况下，仅重置 cost critic、cost 优化器和 checkpoint 中的 \(\lambda\)；
- CPU（中央处理器）测试共 19 项，最近一次全部通过；
- 32 环境、2 次更新的 GPU 冒烟训练已经完整通过。

任务标识为 `Isaac-Pace-Eco-Anymal-D-Flat-v0`。

## 5. 已完成实验与结果

下表中的 explained variance（解释方差，EV）均指外部完整回合评估得到的 cost critic 指标。`B=10000 J` 只是早期占位预算，不是正式论文预算。

| 阶段 | 训练或评估设置 | 主要结果 | 判断 |
| --- | --- | --- | --- |
| GPU 冒烟 | 32 环境，2 更新，`model_1.pt` | 32 回合能量 17264.70 J，成功率 1.0，bias 1.6447，EV -0.0570 | 完整链路跑通，不用于结论 |
| 开发周期 0 | 256 环境，50 更新，`model_49.pt`，B=10000 J | 策略频繁摔倒；评估能量 635.41 J，增广代价 1.25，成功率 0，EV -1528.76 | 乘子由 0 更新到 0.0125，幂等性通过 |
| 开发周期 1 | 再训练 50 更新，`model_98.pt` | 评估能量 1370.44 J，增广代价 1.25，成功率 0，EV -30369.36 | 乘子更新到 0.025，失败屏障主导 |
| 隔夜训练 | 2048 环境，从 `model_98.pt` 继续 3000 更新，最终 `model_3097.pt` | 后 100 次：成功/超时率 0.98185，回合长度 990.74/1000，XY 速度误差 0.16144，能量 26122.24 J；外部 256 回合能量 26072.25 J，成功率 0.99609 | 策略已经能稳定行走；该值仅作开发预算校准 |
| B100、48 步 | 2048 环境，100 更新，未重置旧 cost critic，`model_3196.pt` | 外部能量 25446.27 J，成功率 1.0，bias 1.46551，EV -1.23920 | 未通过；旧 10000 J 尺度被继承 |
| B100、64 步 | 2048 环境，100 更新，选择性重置 cost critic，`model_3295.pt` | 外部能量 26315.10 J，成功率 1.0，bias 0.43421，EV -1.42496 | 仍未通过；不是简单延长 rollout 就能解决 |
| 49 维配对离线对照 | 冻结 `model_3097.pt`，256 完整回合；零时间列/真实时间列各 3 个配对初始化种子 | 真实时间组全轨迹 RMSE `0.01083`、EV `0.99884`；零时间组 RMSE `0.26242`、EV `0.19187`；time-only RMSE `0.00600`、EV `0.99958` | 剩余时间假设得到强支持；当前稳定策略的 cost-to-go 主要由剩余时长决定 |
| critic-only 预热 | 2048 环境、64 步 rollout、100 更新，离线 seed 0 warmstart，最终 `model_3196.pt` | actor 与 reward critic 均 0 个张量变化，动作标准差不变；cost critic 8/8 个张量更新；末次训练 EV `0.98906` | 冻结不变量通过，允许进入固定 \(\lambda=0\) 的解冻诊断 |
| actor 解冻诊断 | 从预热 `model_3196.pt` 恢复，固定 \(\lambda=0\)，100 更新，最终 `model_3295.pt` | 后 20 次训练 EV 均值 `0.98042`，末次 EV `0.98949`，末次 cost value loss `2.08e-5` | 在线短段训练表面稳定，仍需独立完整回合校准 |
| P0 双评估 | 同一 `model_3295.pt`，256 回合，seed `24680/97531` | 初态绝对 bias `0.15454/0.15536`，初态 RMSE `0.15460/0.15626`，全轨迹 EV `0.94563/0.94526`，回合等权 RMSE `0.07892/0.08313` | **未通过**；EV 通过但绝对值系统性低估，禁止恢复乘子更新 |

隔夜训练稳定策略产生的开发期预算为：

- 暂定 \(B_{100}=26072.25390625\,\mathrm{J}\)；
- 暂定 \(B_{90}=23465.028515625\,\mathrm{J}\)；
- 暂定 \(B_{80}=20857.803125\,\mathrm{J}\)。

这些预算不能直接作为最终论文预算。`model_3097.pt` 是在占位预算 10000 J、固定 \(\lambda=0.025\) 下训练出的开发策略，并非严格的无约束或原始 PACE 基线。正式预算需要在基线实现和配置冻结后重新标定。

## 6. 当前关键问题

### 6.1 已确认现象

- actor 已能学到稳定步态，问题不是“策略完全训练失败”；
- 48 步和 64 步 rollout 的 cost critic 都未达到原定门槛；
- 训练日志中的短段 EV 有时较高，但独立完整回合评估的 EV 接近 0 或为负，二者不能混用；
- 当前 B100/64 的外部 bias 为 0.4342，明显高于 0.05 门槛；
- 当前评估失败，因此不能用该结果更新 \(\lambda\)。

### 6.2 最可能的结构性原因

当前 actor、reward critic 和 cost critic 都使用同一组 48 维 `policy` 观察，观察中没有回合进度或剩余时间。

cost critic 需要预测有限 20 秒回合内的“剩余累计能量”。相同身体状态如果出现在第 1 秒和第 19 秒，未来剩余能量不同；但当前网络无法从观察中区分这两个时刻。因此对 cost value（代价值）而言，状态存在时间混叠，不满足有限时域问题所需的马尔可夫表达。

这解释了为什么仅将 rollout 从 48 增加到 64 没有解决外部校准问题。

### 6.3 时间假设已经证实

冻结 `model_3097.pt` 得到的 256 条完整轨迹已经完成配对对照。真实剩余时间组相对恒定零时间列将全轨迹 RMSE 降低约 95.9%，EV 从约 0.192 提升到约 0.9988。time-only 线性基线表现更好，说明当前稳定步态的剩余累计代价主要由剩余时长决定。这是有效研究结果，不要求 49 维网络必须显著优于 time-only。

### 6.4 新确认的在线校准漂移

加入时间特征、离线预热并固定 \(\lambda=0\) 解冻训练后，两次独立完整回合评估仍得到近乎相同的系统性低估：

\[
\hat V_C(s_0)\approx 0.837,\qquad G_C(s_0)\approx 0.992.
\]

全轨迹 EV 约 0.945，说明曲线形状已学到；但初态 bias 约 \(-0.155\)，说明绝对标定丢失。EV 使用误差方差，不能检测近似常数偏移，所以训练日志中的高 EV 不能替代 bias 和 RMSE 门槛。

进一步离线回放同一旧数据得到：离线 seed 0 初态 signed bias 为 `+0.0236`，critic-only 预热 checkpoint 为 `-0.0100`，解冻 100 更新后的 checkpoint 漂移到 `-0.1664`。因此问题主要发生在在线分段更新，而不是离线预热失败。

最可能的机制是：PACE 约束环境必须从真实 reset 开始，2048 个环境的回合相位基本同步；每个 64 步 rollout 只覆盖有限时间区间。同时 `cost_gamma=1` 时，非终止 Bellman/GAE target 对加性常数偏移约束很弱，只有稀少且时间上集中的终止样本能够锚定绝对零点。当前结果与“形状正确、整体下移”的现象一致。

## 7. 当前产物位置

### 7.1 代码与计划

- 详细设计计划：[`docs/PACE_ECO_实现计划.md`](docs/PACE_ECO_实现计划.md)
- 当前现状：本文件
- PPO-Lagrangian 实现：[`source/pace_sim2real/pace_sim2real/algorithms/ppo_lagrangian.py`](source/pace_sim2real/pace_sim2real/algorithms/ppo_lagrangian.py)
- 能耗环境：[`source/pace_sim2real/pace_sim2real/tasks/manager_based/pace/pace_energy_env.py`](source/pace_sim2real/pace_sim2real/tasks/manager_based/pace/pace_energy_env.py)

旧计划文档中的部分勾选状态和 V100 命令已落后于当前实验；本文件是截至 2026-07-26 的权威状态快照。

### 7.2 关键模型

- 冒烟模型：`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-24_22-37-58/model_1.pt`
- 开发周期 0：`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-24_22-57-02_dev_k50_seed42/model_49.pt`
- 开发周期 1：`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-24_23-05-22_dev_cycle1_eta005_seed42/model_98.pt`
- 稳定隔夜策略：`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-24_23-22-05_overnight_eta005_seed42/model_3097.pt`
- B100、48 步：`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-25_21-11-38_calib_b100_rollout48_cycle0_seed42/model_3196.pt`
- B100、64 步并重置 cost critic：`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-25_21-49-13_calib_b100_rollout64_resetcost_cycle0_seed42/model_3295.pt`
- P0 critic-only 预热：`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-26_15-19-29_p0_cost_critic_preheat_seed42/model_3196.pt`
- P0 actor 解冻且固定乘子：`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-26_15-38-42_p0_actor_unfreeze_lambda0_seed42/model_3295.pt`

### 7.3 关键评估结果

- 开发对偶评估：`logs/dual_dev/cycle_0000`、`cycle_0001`、`cycle_0002`
- B100、48 步：`logs/calibration_b100/dual/cycle_0000/result.json`
- B100、64 步：`logs/calibration_b100/dual64/cycle_0000/result.json`
- 当前开发乘子状态：`logs/calibration_b100/state_eta005.json`
- 完整轨迹数据：`data/pace_eco/cost_critic_model3097_seed24680.pt`
- 49 维配对对照：`logs/cost_critic_comparison/model3097_seed24680/comparison.json`
- P0 双评估：`logs/p0_cost_critic_gate_model3295/dual/cycle_0000/result.json`、`cycle_0001/result.json`

## 8. 后续计划

### P0：先解决 cost critic 的可辨识性

详细冻结协议及命令见 [`docs/P0_代价价值网络校准协议.md`](docs/P0_代价价值网络校准协议.md)。当前顺序为：

1. `dual-eval` 分别报告初态指标和完整轨迹全部有效时间步指标，目标使用真实 undiscounted return-to-go（无折扣剩余累计代价）。
2. 冻结 `model_3097.pt`，一次采集完整轨迹，再按完整回合划分训练集和留出集。
3. 在完全相同数据上运行三组对照：48 维状态加恒定零、48 维状态加真实剩余时间、只使用剩余时间的线性基线。前两组均为 49 输入并使用三个配对初始化种子。
4. 若对照支持时间假设，选择留出集表现合适的真实时间 critic，执行显式 critic-only 预热；用哈希确认 actor、reward critic 和动作标准差逐位不变。
5. 解冻 actor，固定 \(\lambda=0\) 运行 100 次更新。
6. 对同一 checkpoint 使用两个不同评估 seed；通过完整轨迹门槛后才恢复乘子更新。
7. 主方法显式采用与 ECO 源码一致的全 rollout cost advantage 标准化，并保留不标准化消融。

归一化剩余时间定义为：

\[
t_{\mathrm{remain}}=1-\frac{\mathrm{episode\_length}}{\mathrm{max\_episode\_length}}.
\]

cost critic 使用现有 48 维 `policy` 观察加独立 1 维 `cost_time`；actor 和 reward critic 继续只使用原 48 维观察。

进入下一阶段的暂定门槛：

- \(|V_C(s_0)-\overline C|\le 0.05\)；
- pooled 全轨迹 EV≥0.5；
- 连续两次 dual-eval 都通过；
- checkpoint 哈希、cycle ID 和幂等更新检查通过；
- 所有权重和指标保持有限值。

如果新增时间观测后 bias 明显改善、但 EV 仍受随机回报噪声限制，则先分析诊断量，再决定是否修订 EV 门槛。决策与理由必须写入实验记录。

### P1：冻结正式实验协议

P0 通过后执行：

1. 完成连续训练与每 100 次更新分段重启的等价性测试。
2. 完成原始 PACE 固定能耗 reward/curriculum 基线。
3. 对固定能耗权重做充分扫描，不能只选择一个弱基线。
4. 用无约束或正式基线策略重新标定 \(B_{100}\)，再得到 \(B_{90}\) 和 \(B_{80}\)。
5. 在开发种子上扫描乘子学习率 \(\eta_\lambda\in\{0.01,0.05,0.1\}\)。
6. 冻结 rollout 长度、网络、预算、评估方式、乘子学习率和训练迭代上限。

### P2：正式多种子实验

冻结配置后再运行正式种子 `0,1,2`，至少比较：

1. 原始 PACE 固定能耗 reward/curriculum；
2. 扫描后选定固定权重的 PPO；
3. PACE 物理能耗模型 + PPO-Lagrangian。

每组使用同一批随机初态：

- 训练中周期评估使用 32 回合；
- 最终冻结 checkpoint 使用 200 个配对初态，只测试一次；
- 统计独立单位以训练种子为主，不能把 `3×200` 写成 600 个独立策略样本。

主要指标：

- 速度跟踪误差；
- 成功率和摔倒率；
- 模型能量与焦耳/米；
- 回合能量违反比例、成功回合平均相对超预算幅度和联合可行率；
- 拉格朗日乘子轨迹；
- cost critic 校准指标；
- 参数扰动压力集下的稳健性。

### P3：论文结论与消融

1. 比较 B100、B90、B80 三档预算下的性能—能耗折中。
2. 消融失败屏障、时间观察、乘子学习率和参数扰动。
3. 将 `params_buffer` 表述为“辨识相容压力集”，不称为参数后验或置信区间。
4. 所有结论限定为辨识后仿真模型，不外推真实电池或真机部署保证。

## 9. 当前建议的立即行动

P0 已正式通过。P1 已在冻结协议下恢复开发运行，但三个乘子学习率仍未完成等周期比较。\(\eta_\lambda=0.01\) 的 cycle 1 已在延长 cost critic 校准后通过双 seed 门槛并完成一次幂等 \(\lambda\) 更新；当前下一项是该分支的 cycle 2 actor 训练。

dual-eval 结果正在升级为 schema v3，新增逐回合能量违反比例、成功条件违反比例和联合可行率；旧 v1/v2 结果保持可读。现有完整回合数据已通过 CPU 工具补算，并保存到 `logs/p1_b90_metric_backfill/`，不覆盖原始评估。

下一项 GPU 操作必须遵循 [`docs/P1_B90_开发选择与停止协议.md`](docs/P1_B90_开发选择与停止协议.md)：先将 \(\eta_\lambda\in\{0.01,0.05,0.10\}\) 补齐到各 3 个周期，再按冻结规则选择开发值。正式论文预算还要由从随机初始化训练的标准 PPO 干净基线重新标定；在此之前不启动 B80 或正式种子 `0,1,2`。

GPU 训练使用 A100，例如命令前缀：

```bash
CUDA_VISIBLE_DEVICES=0 python scripts/rsl_rl/train.py ... --device cuda:0
```

不要使用当前环境中的 V100 跑训练；这与 RTX（实时光线追踪）渲染无关，而是当前 PyTorch CUDA 构建不支持 V100 的计算架构。PACE 的无界面物理训练不要求 RTX 显卡。

## 10. 当前验收状态

| 门槛 | 状态 |
| --- | --- |
| 400 Hz 子步能量累计 | 已通过代码与冒烟验证 |
| 编码器偏置语义 | 已核对并固化 |
| cost timeout 语义 | 已实现并有测试 |
| RSL-RL 5.0.1 / Isaac Lab 兼容 | 已通过 GPU 训练 |
| checkpoint 恢复与乘子幂等 | 已通过 |
| 稳定步态策略 | 已获得开发策略 `model_3097.pt` |
| v2 全轨迹指标与配对对照基础设施 | 已实现并完成 GPU 数据对照；时间假设通过 |
| critic-only 冻结不变量 | 已通过；actor、reward critic 和动作标准差逐位不变 |
| 固定 \(\lambda=0\) 解冻诊断 | 已完成；确认在线绝对标定漂移 |
| 终端边界、MC replay 与初态 MC 锚点 | 已完成；默认关闭，28 项测试通过 |
| cost critic 连续两次校准 | **已通过：两个独立 seed 的 bias、RMSE、EV 全部门槛通过** |
| 连续/分段协议 | 不作单 seed 等价主张；三组方法统一使用 100-update 分段重启 |
| 预算候选 | 25838.29/23254.47/20670.64 J 仅用于当前开发；正式预算须由干净标准 PPO 基线重标定 |
| 正式三种子实验 | 尚未开始 |
| 真实机器人验证 | 不在当前研究范围内 |

因此，项目状态是：**P0 cost critic 校准已经通过；P1 的 B90 尚未满足，\(\eta_\lambda=0.05\) 仍仅是暂定开发值；\(\eta_\lambda=0.01\) 已完成 cycle 1，下一步进入 cycle 2。**

## 11. 2026-07-26 P0 最终通过记录

冻结 actor 的边界对照确认，显式终端条件

\[
V_C(s,t_{\mathrm{remain}}=0)=0
\]

能将 pooled 全轨迹 RMSE 从约 `0.06636` 降到 `0.02561`，EV 从约 `0.96382` 提升到 `0.99498`。但固定 \(\lambda=0\) 解冻 actor 100 次更新后，初态仍存在约 `-0.052` 的系统偏差。

随后使用当前 `model_3295.pt` 随机策略新采集 256 个完整回合，并加入完整回合 Monte Carlo（蒙特卡洛）return-to-go 辅助损失。单纯均匀采样能把全轨迹 RMSE 降到约 `0.008`，但精确 reset 初态只占全部 255,029 个 transition 的约 `0.1%`，导致初态偏差反向变成约 `+0.060`。数据索引审计确认 observation、cost 和剩余时间没有错位。

最终配置在均匀 MC replay 之外增加显式初态 MC 锚点：每次 cost-critic 更新额外监督数据集中的全部 256 个 reset 状态。该项只更新 cost critic，不进入 actor loss、reward critic 或 \(\lambda\) 更新。最终 checkpoint 为：

`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-26_17-46-42_p0_boundary_b1_mc1_initial1_calib_seed42/model_3319.pt`

权重审计结果：actor 和 reward critic 均有 0 个张量变化，cost critic 的 8 个张量更新，\(\lambda=0\)，所有权重有限。两个独立留出 seed 使用同一 checkpoint SHA-256 `eb8903a8120ee82da3890aa1d77035e5cb28ec5a2429d208b84c1ae66a4a1783`：

| 指标 | seed 24680 | seed 97531 | 门槛 |
| --- | ---: | ---: | ---: |
| 初态 signed bias | +0.00063 | +0.00005 | 绝对值 ≤0.05 |
| 初态 RMSE | 0.00478 | 0.01646 | ≤0.05 |
| pooled 全轨迹 EV | 0.99962 | 0.99940 | ≥0.5 |
| pooled 全轨迹 RMSE | 0.00563 | 0.00704 | 诊断 |
| 回合等权 RMSE | 0.00563 | 0.01750 | ≤0.05 |
| 成功率 | 1.00000 | 0.99609 | 诊断 |

`check_cost_critic_gate.py` 已正式返回 `PASS`。P0 连续两次校准门槛已经通过，本轮没有据此修改 \(\lambda\)。关键新产物：

- 新鲜 MC 数据：`data/pace_eco/cost_critic_boundary_model3295_seed13579.pt`
- 最终评估：`logs/p0_boundary_mc_initial_gate_model3319/dual/cycle_0000/result.json`、`cycle_0001/result.json`

下一阶段进入 P1：主方法固定采用 ECO 源码一致的全 rollout cost advantage 标准化，未标准化版本只作消融；所有方法统一使用每 100 次更新分段重启，再重标定 \(B_{100}\)、\(B_{90}\)、\(B_{80}\) 和扫描乘子学习率。正式约束周期不得跨策略长期复用 MC 数据。本段计划的最新执行结果见第 12 节。

## 12. 2026-07-26 P1 预算与乘子学习率开发结果

### 12.1 统一分段协议与预算

不再用单个 seed 声称“连续训练与分段训练等价”。三类正式方法统一采用每 100 次更新分段重启，因此 restart 是共同控制变量：

1. 固定 \(\lambda\) 训练 actor 100 次更新；
2. 从当前不可变 actor checkpoint 采集新鲜完整回合；
3. critic-only 校准 cost critic；
4. 使用两个不同 seed 做完整回合校准门槛；
5. 仅在门槛通过后幂等更新一次 \(\lambda\)。

当前开发谱系的 `model_3295.pt` 在 seed 13579 的 255 个成功完整回合中，平均物理能耗为 `25838.2949 J`、标准差为 `103.4899 J`。提前摔倒的单个回合只有 `598.8 J`，已从物理预算均值排除，避免把失败误当节能。该 checkpoint 继承了早期占位约束训练谱系，因此下列数值只作为开发候选，不能作为论文正式预算：

\[
B_{100}=25838.2949\,\mathrm J,
\quad B_{90}=23254.4654\,\mathrm J,
\quad B_{80}=20670.6359\,\mathrm J.
\]

B90 新鲜数据为 `data/pace_eco/cost_critic_model3295_b90_seed13579.pt`。重置 cost critic 后使用终端边界、均匀 MC 和初态 MC 锚点校准得到：

`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-26_18-01-30_p1_b90_cost_calib_mc1_initial1_seed42/model_3344.pt`

两个独立 seed 的 B90 门槛均通过：初态 RMSE `0.00778/0.01157`，pooled 全轨迹 EV `0.98459/0.98456`，回合等权 RMSE `0.04133/0.04265`。

### 12.2 \(\eta_\lambda\) 一周期配对筛选

三个分支均从同一个 B90 `model_3344.pt`、训练 seed 42、2048 环境和 64 步 rollout 开始，各运行 100 次更新；段末都使用 seed 13579 的 256 个完整回合。成功回合物理能耗结果为：

| 分支 | 初始 \(\lambda\) | 成功回合 | 成功回合平均能耗 [J] | 成功回合平均相对超预算幅度 | 回合能量违反比例 | 联合可行率 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 开发起点 | 0 | 255/256 | 25838.295 | 11.1111% | 99.6094% | 0% |
| \(\eta=0.01\)，cycle 1 | 0.00111499 | 253/256 | 26114.186 | 12.2975% | 98.8281% | 0% |
| \(\eta=0.05\)，cycle 1 | 0.00557495 | 256/256 | 25543.475 | 9.8433% | 100% | 0% |
| \(\eta=0.10\)，cycle 1 | 0.01114991 | 256/256 | 25797.869 | 10.9373% | 100% | 0% |
| \(\eta=0.05\)，cycle 2 | 0.01052714 | 256/256 | 25558.053 | 9.9060% | 100% | 0% |

一周期探索结果中，`0.05` 同时给出最低平均能耗和完整成功率，因此只暂定为继续调试闭环的开发值。更大的 `0.10` 没有产生更低能耗，说明响应不单调；正式冻结学习率前必须完成等周期比较。

`0.05` 分支已完整运行并审计两个周期：

- cycle 1 正式/audit 能耗 `25499.58/25453.87 J`，成功率均为 `99.609%`，校准门槛通过；
- cycle 2 正式/audit 能耗 `25479.46/25566.47 J`，成功率 `99.219%/100%`，校准门槛通过；
- 状态文件 `logs/p1_b90_eta005/state.json` 已幂等更新到 cycle 2、\(\lambda=0.01550687\)；重复应用均返回 `changed=False`。

因此当前结果不是“B90 已满足”或“正在明确收敛”，而是：\(\eta=0.05\) cycle 1/2 相对开发 B100 仅节能约 `1.14%/1.08%`；成功回合平均相对超预算幅度从 `9.8433%` 变为 `9.9060%`，应判为基本持平。离线逐回合补算还显示两个周期的回合能量违反比例均为 `100%`，联合可行率均为 `0%`。

### 12.3 当前关键产物

- B90 初始 critic：`2026-07-26_18-01-30_p1_b90_cost_calib_mc1_initial1_seed42/model_3344.pt`
- \(\eta=0.05\) cycle 1 actor：`2026-07-26_18-13-44_p1_b90_eta005_cycle1_seed42/model_3443.pt`
- \(\eta=0.05\) cycle 1 校准：`2026-07-26_18-26-24_p1_b90_eta005_cycle1_cost_calib_seed42/model_3467.pt`
- \(\eta=0.05\) cycle 2 actor：`2026-07-26_18-36-49_p1_b90_eta005_cycle2_seed42/model_3566.pt`
- \(\eta=0.05\) cycle 2 校准：`2026-07-26_18-49-43_p1_b90_eta005_cycle2_cost_calib_seed42/model_3590.pt`
- \(\eta=0.10\) cycle 1 actor：`2026-07-26_18-57-49_p1_b90_eta01_cycle1_seed42/model_3443.pt`
- \(\eta=0.01\) cycle 1 actor：`2026-07-26_19-10-42_p1_b90_eta001_cycle1_seed42/model_3443.pt`
- \(\eta=0.01\) cycle 1 最终校准：`2026-07-26_21-55-15_p1_b90_eta001_cycle1_cost_recalib50_seed42/model_3492.pt`
- \(\eta=0.01\) cycle 1 v3 主/审计评估：`logs/p1_b90_eta001/recalib50_cycle1/dual/cycle_0001/result.json`、`logs/p1_b90_eta001/recalib50_cycle1_audit/dual/cycle_0002/result.json`
- \(\eta=0.01\) 正式状态：`logs/p1_b90_eta001/state.json`
- \(\eta=0.05\) 正式状态与评估：`logs/p1_b90_eta005/state.json`、`logs/p1_b90_eta005/dual/`

### 12.4 下一步

1. 从 \(\eta=0.01\) 的最终校准 checkpoint `model_3492.pt` 和 cycle 1 状态开始训练 actor cycle 2；随后继续完整回合采集、cost critic 校准和双 seed 评估。
2. 按新协议把 \(\eta=0.01,0.05,0.10\) 都补齐到 3 个完整周期，再选择开发学习率；不再单独延长 `0.05`。
3. 选定分支至少运行 5 个、最多 10 个周期；连续 3 周期满足平均增广代价和成功率门槛才判定 B90 通过。
4. 成功率下限、平台停止和上限停止规则已固定在 [`docs/P1_B90_开发选择与停止协议.md`](docs/P1_B90_开发选择与停止协议.md)。
5. 使用新任务 `Isaac-Pace-Eco-Anymal-D-Flat-Unconstrained-v0` 从随机初始化训练标准 PPO，重标定论文正式 B100/B90/B80；旧 dual state 不迁移。
6. 在开发分支通过且正式预算生成前不启动 B80，也不启动正式种子 `0,1,2`。
7. 补充未标准化 cost advantage 消融，但主方法继续固定为 ECO 一致的全 rollout 标准化。
8. 所有固定权重 PPO 与 PACE reward/curriculum 基线采用同样 100-update 分段重启协议。

本轮新增 dual-eval schema v3、CPU 离线补算工具和标准 PPO 干净基线任务；新结果将直接记录真实回合能量违反比例与联合可行率。CPU 测试 `28/28`、Python 静态编译和 `git diff --check` 均通过。AppLauncher 注册冒烟检查未完成：当前两张 A100 均被其他进程占用约 62 GB，同时 Isaac Sim 报 CUDA 上下文初始化失败；二者是否存在直接因果尚未唯一确认。PyTorch 的 CUDA 枚举和实际小规模内核运算均可用。所有 GPU checkpoint 权重均有限；正式乘子状态仍只允许使用通过双 seed 校准门槛的结果更新。

### 12.5 2026-07-27：\(\eta_\lambda=0.01\) cycle 1 正式完成

cycle 1 actor checkpoint 为：

`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-26_19-10-42_p1_b90_eta001_cycle1_seed42/model_3443.pt`

使用 seed 13579 的新鲜完整回合数据和 `终端边界 + MC replay + 初态 MC 锚点` 做 25 次 critic-only 校准后，两个评估 seed 的初态 bias、初态 RMSE 和全轨迹 EV 都通过，但回合等权 RMSE 为 `0.06837/0.06669`，超过 `0.05` 门槛。双 seed gate 正式返回 `FAIL`，该结果未用于更新 \(\lambda\)。失败评估保留在：

- `logs/p1_b90_eta001/dual/cycle_0001/result.json`
- `logs/p1_b90_eta001/audit_cycle1/dual/cycle_0002/result.json`

训练曲线显示第 25 次时 MC 损失和终端边界误差仍在下降，因此保持监督定义与权重不变，从原 actor checkpoint 重新执行 50 次 critic-only 校准。最终 checkpoint 为：

`logs/rsl_rl/pace_eco_anymal_d_flat/2026-07-26_21-55-15_p1_b90_eta001_cycle1_cost_recalib50_seed42/model_3492.pt`

逐张量审计确认 actor（含动作标准差）和 reward critic 完全不变，只有 cost critic 更新；checkpoint 中 \(\lambda=0\)，三个校准系数均为 1.0。末段 MC 损失降至 `0.000165`，在线 cost EV 约为 `0.9773`。

重新评估结果如下：

| 指标 | seed 24680 | seed 97531 | 门槛 |
| --- | ---: | ---: | ---: |
| 初态 absolute mean bias | 0.00284 | 0.00109 | ≤0.05 |
| 初态 RMSE | 0.01658 | 0.01206 | ≤0.05 |
| pooled 全轨迹 EV | 0.99770 | 0.99889 | ≥0.5 |
| 回合等权 RMSE | 0.02176 | 0.01831 | ≤0.05 |
| 成功率 | 0.98438 | 0.99219 | ≥0.98 |
| 平均增广代价 | 1.12506 | 1.12344 | ≤1.0 才满足 B90 |
| 成功回合平均相对超预算幅度 | 12.3075% | 12.2444% | 诊断 |
| 回合能量违反比例 | 98.8281% | 99.2188% | 诊断 |
| 联合可行率 | 0% | 0% | 诊断 |

双 seed gate 返回 `PASS`。仅主评估结果被原子、幂等地应用到 `logs/p1_b90_eta001/state.json`：

\[
\lambda: 0.00111499 \rightarrow 0.00236557,
\qquad \text{cycle}:0\rightarrow1.
\]

首次应用返回 `changed=True`，重复应用返回 `changed=False`。因此 \(\eta_\lambda=0.01\) 的 cycle 1 已正式完成，但 B90 明确未满足。下一项尚未执行的 GPU 工作是从 `model_3492.pt` 和该 cycle 1 dual state 训练 100 次 actor 更新，预计生成 cycle 2 的 `model_3591.pt`。
