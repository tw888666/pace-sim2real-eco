# PACE-ECO 多地形 PPO seed 复用协议修订

> 历史说明：本文记录 v1.2。v1.3 继续使用 seed0 标定、seed1–5 正式配对，但已取消
> 15 m 完成线、提前终止和完成奖励；现行规则以“仅改变地形”修订文档和冻结配置为准。

修订日期：2026-08-05 CST

协议变更：`gpt-multi-terrain-v1.1` → `gpt-multi-terrain-v1.2`

## 1. 决定

为与既有平地实验的 PPO seed（随机种子）角色保持一致，并强化同 seed 配对的可解释性，
阶段一和阶段二均使用：

- 任务型 PPO seed0：训练预算参考模型并在 calibration 上冻结各地形 `B_ref,t`；
- seed1–5：任务型 PPO、PACE 论文系数固定权重 PPO 和 PACE-ECO 的正式配对训练；
- seed900/901：仅保留为历史/可选 GPU 冒烟角色，不进入预算或正式统计。

阶段一五类地形、阶段二混合任务和三种方法都从随机初始化训练，禁止复用旧检查点。

## 2. “主要只改变地形”的准确范围

在新阶段一走廊实验内部，三种方法的正式 PPO seed、网络、速度、摩擦、动力学、训练
长度、地形课程规则和评估状态全部配对；五类任务的主要实验变量是地形生成分布。

与历史 Flat 正式实验相比，新实验还改变了任务几何与回合协议：使用 24 m 固定走廊、
15 m 完成线、提前完成终止和完成奖励。因此相同 seed1–5 增强了配对可解释性，但不能
证明历史 Flat 与新 corridor-flat 之间“只有地形不同”，也不能把旧模型或结果并入
v1.2 正式统计。

## 3. B_ref 的单 seed 限制

每类 `B_ref,t` 定义为任务型 PPO seed0 在冻结 calibration 新几何上的成功回合平均
能耗，并要求成功率至少 95%。正式 seed1–5 不参与阈值标定，也不得在正式或 holdout
结果出现后用于修改 `B_ref,t` 或 `B80,t`。

单一训练 seed 与既有平地标定角色一致，但不能估计预算参考的训练 seed 间方差。报告
必须明确这一限制；240 个 calibration 几何回合只能估计同一策略的几何实例变化，不能
替代多个训练 seed 的不确定性。

## 4. 隔离与配对

复用 PPO seed 数值不等于复用数据。以下隔离保持不变：

- 阶段、地形、方法使用独立任务 ID 和模型目录；
- budget 训练、正式训练、calibration 和 holdout 使用不相交的六位地形 seed 段；
- 同一阶段、地形和正式 PPO seed 下，三种方法使用相同地形 seed；
- 阶段一与阶段二不合并为独立样本，正式统计仍分别报告；
- v1.2 输出写入 `multi_terrain_v1_2`，不覆盖 v1/v1.1 或历史平地结果。

## 5. 工作量

- 阶段一：5 个 B_ref 模型 + 75 个正式模型 = 80 次训练；
- 阶段二：1 个 B_ref 模型 + 15 个正式模型 = 16 次训练；
- 合计：96 次训练和 96 次评估。

暂估总量约 300–790 GPU·小时。该值是容量规划估计，不是正式长训练实测。

## 6. 停止点

本修订不授权 GPU 正式训练或评估。完成非 GPU 回归、由 nullptr 审阅工作树并处理为
干净状态、确认 GPU 资源安全且获得正式训练确认后，才可启动阶段一 flat/task_only/
seed0 的 B_ref 训练。
