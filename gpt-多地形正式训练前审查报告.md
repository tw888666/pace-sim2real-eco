# PACE-ECO 多地形正式训练前审查报告

> 已归档：本报告面向 v1.2，不能授权 v1.3 Terrain20s 正式训练。现行执行关口见 v1.3
> 执行清单。2026-08-06 Terrain20s 五类 GPU 冒烟与状态审计已经通过，现行证据见
> `gpt-多地形Terrain20s-GPU冒烟验证报告.md` 和
> `gpt-多地形Terrain20s仿真状态审计报告.md`；本归档报告不能授权 v1.3 正式训练。

> 历史说明：本报告记录 `gpt-multi-terrain-v1` 审查快照。2026-08-05 用户决定固定权重
> 先统一使用 PACE 论文 ANYmal 系数 W100=-0.00016；当前主协议已修订为 v1.1。地形与
> 仿真审计证据继续有效，固定权重选择联锁已由“论文系数常量联锁”替代。
> 随后的 v1.2 又将 B_ref/正式 PPO seed 改为 0/1–5；本报告中的 seed100 启动点已经失效。

协议版本：`gpt-multi-terrain-v1`

审查时间：2026-08-05 CST

## 1. 结论

五类静态几何、任务型 PPO（近端策略优化）GPU（图形处理器）链路冒烟和实际碰撞网格
状态审计均已通过。正式训练前代码审查又发现并修复了评估身份联锁、冻结证据联锁、
零成功统计和基础设施中断恢复四类风险。最终 58 项非 GPU 测试通过。

当前实现已达到“可由 nullptr 审阅并提交”的状态，但尚不能启动 3000 次训练：工作树有
43 个修改/新增条目且未提交，训练脚本会主动拒绝；同时 GPU0–4 当前均被其他进程占用
或处于高计算负载。未绕过任何安全门，未启动正式训练或评估。

## 2. 本轮审查修复

### 评估身份

- calibration（校准）和 holdout（留出集）只接受最终 `model_2999.pt`；
- 命令中的 PPO seed 必须与检查点复现记录中的 seed 完全一致；
- 正式多地形评估拒绝训练时 Git（版本控制系统）工作树不干净的模型；
- 继续保留任务 ID、实现源码哈希、预算和固定输入哈希校验。

### 冻结证据

- B_ref、固定权重选择和 holdout 授权现在同时校验冻结状态、协议版本与实验阶段；
- 固定权重选择文件必须由授权时提供的同一份 B_ref 文件生成；
- holdout 评估提供的 B_ref 文件必须与授权清单中的 SHA256（安全哈希）一致；
- 授权文件新增协议版本字段，防止跨协议混用。

### 最终统计

- 汇总入口新增必填 `--stage`，硬检查三种方法、五类地形、五个正式配对 seed 和冻结
  回合数；缺失、重复、混入 calibration 或未配对时拒绝输出；
- 增加逐 seed 的成功/失败回合数、四类失败原因计数及失败能耗均值、样本标准差和范围；
- 某 seed 零成功时不再崩溃或静默剔除：相关成功回合能耗和配对节能明确记为不可计算，
  同时保留成功率、联合合格率和全部失败证据；
- 跨地形能耗宏平均仅在五类均可计算时给出，否则返回空值并记录覆盖地形数。

### 中断恢复

`运行多地形训练.sh` 现在允许在原参数末尾追加同一次运行的检查点。恢复仍要求 tmux
或 GNU Screen（可断线恢复终端）、干净工作树及完全一致的任务、seed、地形 seed、
预算、配置指纹和源码哈希；不能跨运行拼接模型。

## 3. 状态同步与验证

- 机器可读冻结配置已同步为“GPU 冒烟与状态审计通过、正式运行未授权”；
- 冻结配置 JSON（结构化配置）SHA256：
  `ca41d8caec0a309d7ff5cdefbefe4e6507198833da510f7f8fb11f4b130f3314`；
- 纯 CPU 几何审计 SHA256 保持：
  `7e7b9472433e40a2f2714c0549a6784ce9f0bda3e71f6d8c64e872f232baa510`；
- 58 项非 GPU 测试通过；
- 全部 Python 源码编译通过；
- 全部 shell（命令解释器）脚本 `bash -n` 通过；
- 全部 `gpt-*.json` 语法解析通过；
- `git diff --check` 通过；
- 本机未安装 `shellcheck`，因此没有执行该附加检查。这不是现有项目的必需测试，且所有
  shell 入口已通过 Bash 自带语法检查。

## 4. 当前资源与停止点

收尾快照中，GPU0 仅剩约 24.9 GiB、GPU1 约 15.9 GiB、GPU2 约 3.2 GiB；GPU3 虽有
约 31.3 GiB 空闲但计算利用率约 98%，GPU4 有其他长期训练，GPU5 按项目约束不可用。
因此没有一张卡适合现在启动 4096 环境的正式长训练。

下一步必须由 nullptr 审阅 43 个工作树条目并自行决定 commit（提交）。提交后重新查询
GPU、tmux 和进程；只有获得正式训练确认且出现安全设备，才从阶段一 flat/task_only/
seed100 的 B_ref 训练开始。
