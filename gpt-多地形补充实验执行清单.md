# PACE-ECO 多地形补充实验执行清单

协议版本：`gpt-multi-terrain-v1.4`（Terrain20sWide，仅改变地形）

## A. v1.4 正式训练前

- [x] 使用独立worktree（工作树）和`exp/multi-terrain-comparison-v1`分支；原工作树未改动。
- [x] 新任务ID使用`Terrain20sWide`，输出根使用`multi_terrain_v1_4`。
- [x] 65×60 m块、origin `(30,30)`、29 m有效几何和6 m尾部平台已编码。
- [x] 任一接触足真实越界硬拒绝；机身前30/后2/侧3 m仅预警。
- [x] 训练5×10=50实例；混合比例在10列中精确实现。
- [x] 评估4批×50环境=200实例、批次seed和初始状态偏移已编码。
- [x] v1.3任务、seed、输出、权重和结果不会被自动加载或覆盖。
- [x] 固定权重冻结为PACE论文`W100=-0.00016`，选择入口拒绝运行。
- [x] seed0/seed1–5角色和v1.4独立地形seed段已编码。
- [x] 逐回合类别、方向、难度、批次、实例、距离、能耗分项和四足边界字段已编码。
- [x] CPU几何审计生成新的`gpt-多地形Terrain20sWide几何静态审计.json`；63组全部通过。
- [x] 68项非GPU测试通过；Python编译、shell语法、JSON解析和`git diff --check`通过。
- [x] 审阅并完成干净Git（版本控制系统）commit（提交）；不push（推送）。
- [x] 启动前重新只读检查GPU、tmux和训练进程，未干扰他人任务。
- [x] 经nullptr确认后，五类v1.4 GPU冒烟全部通过，无显存不足。
- [x] 五类实际碰撞网格状态审计通过：5×10 origin、出生无穿透、方向、有限平台、难度分层、
  无性能课程、仅两个历史终止项和四足位置源一致。
- [x] 最重的rough使用正式4096环境完成2次更新容量冒烟，确认当前GPU可承载正式批量。
- [x] 根据v1.4实测更新GPU时间估算；宽场可行，未静默修改任何正式参数。

v1.4 GPU证据、哈希、容量冒烟修复和时间估算见
`gpt-多地形Terrain20sWide-GPU冒烟与状态审计报告.md`。

旧`gpt-多地形Terrain20s-GPU冒烟验证报告.md`和
`gpt-多地形Terrain20s仿真状态审计报告.md`只证明v1.3，不能勾选v1.4 GPU门。

## B. 阶段一 B_ref

- [ ] 五类任务型PPO各从头训练seed0，共5次；不得复用v1.3权重。
- [ ] 每个目录唯一并含`model_2999.pt`、复现记录及环境/算法配置。
- [ ] 五类分别执行4批×50回合calibration，合并为每类200回合。
- [ ] 四批seed互异、全局实例0–199唯一、八个初始状态各25回合。
- [ ] 任一回合接触足越界立即停止；不写正式CSV，不冻结预算。
- [ ] 每地形成功率不低于0.95，否则停止且不得偷换seed。
- [ ] 冻结五个`B_ref,t`和`B80,t=0.8×B_ref,t`并核对输入SHA256（安全哈希）。

## C. 阶段一正式训练和 holdout

- [ ] 五地形×三方法×seed1–5从头训练75个模型。
- [ ] 同地形同seed三方法使用相同地形seed映射。
- [ ] 固定权重恰为−0.00016；PACE-ECO预算恰为对应冻结B80。
- [ ] 不根据曲线、正式seed或holdout调整预算、几何和权重。
- [ ] 75个终点齐备后冻结holdout授权；工具不读取性能。
- [ ] 每个模型执行4批并生成一份200回合结果，共75份、300个GPU评估批次。
- [ ] 所有正式结果零接触足越界，JSON/CSV、摘要、日志和哈希完整。
- [ ] 按seed→地形汇总，五类等权宏平均并报告最差地形、方向和难度。

## D. 阶段二

- [ ] 阶段一协议、阈值、统计规则和结果解释冻结。
- [ ] 三方法通用模型均从随机初始化，不拼接专用模型。
- [ ] 任务型混合seed0训练并执行4批calibration；每类40回合。
- [ ] 冻结五类诊断B_ref的等权宏平均B_ref,mixed及单一B80,mixed。
- [ ] 三方法×seed1–5正式训练15个模型。
- [ ] 15个终点齐备后授权holdout；评估15份×4批并汇总。

## E. 数量与安全

- 阶段一80次训练、80份结果；阶段二16次训练、16份结果。
- 合计96次3000更新训练、96份200回合结果、384个GPU评估进程批次。
- GPU0–5启动前核对占用，同一卡不并发本项目任务；GPU5在2026-08-17服务器重启后已通过CUDA、Isaac Sim/PhysX和rough地形4096环境×2次更新容量冒烟，启用证据见[归档记录](../archive/PACE-ECO-multi-terrain-legacy/gpt-GPU5重启后恢复验证与启用记录.md)。
- 超过1000次更新必须使用现有shell脚本并在tmux或GNU Screen中运行。
- 不停止未知进程，不覆盖结果，不自动stash（暂存工作树）、commit、push或删除文件。
- calibration未冻结前不得训练对应PACE-ECO；阶段一未完成前不得执行阶段二。
