# PACE-ECO 多地形补充实验统计分析规则

协议版本：`gpt-multi-terrain-v1.4`

本规则在任何正式holdout（留出集）前冻结，不得根据结果更换成功定义、预算、排除规则、
聚合顺序或主指标。

## 1. 数据资格和完整性

最小单位是“类别×方法×PPO seed×评估批次×地形实例×初始状态”的单回合。每行必须包含
协议/边界版本、方法、PPO seed、地形基准及批次seed、批次组、全局回合和实例编号、状态集
及哈希、类别、方向、难度、生成参数、20 s/终止状态、速度、距离、能耗三分项、协调性、
路线预警和逐足边界统计。

- 一份正式结果由4批×50环境合并为200回合；批次seed为基准seed加0–3；
- 全局回合与实例编号必须各为0–199且无重复，八个初始状态各25回合；
- 阶段一每份只含一个类别；阶段二每类40回合，上/下楼和上/下坡各20回合；
- 任一回合的接触足中心越过65×60 m真实块边缘，整份评估无效；失败回合也不例外；
- 机身前30/后2/侧3 m和摆动足越界只预警，不直接改变成功；
- 机身距边缘大于1.25 m时由运动学上界证明足端安全，邻边带内读取真实四足位置；
- 不删除失败回合、不补齐缺失值、不挑选同任务同seed的较好重跑。

批次暂存JSON不是正式结果，预算冻结和统计工具只读取四批合并后的正式CSV。

## 2. 成功、预算和联合合格

成功当且仅当：由`time_out`完成固定20 s、没有`base_contact`，且忽略前5 s后的机身坐标系
平均前进速度位于`[0.8,1.2] m/s`。边界审计是整份数据资格，不新增成功条件。

阶段一主预算为对应地形绝对焦耳`B80,t=0.8×B_ref,t`。阶段二为预注册的单一混合预算
`B80,mixed=0.8×B_ref,mixed`。联合合格是“成功且总能耗不超过当前主预算”。阶段二逐类
`E_t/B_ref,t`和诊断B80不得替代主预算。

## 3. B_ref 冻结

- 阶段一：每地形任务型PPO seed0的200个calibration回合中，成功回合能耗算术均值为
  `B_ref,t`；成功率必须至少0.95。
- 阶段二：任务型混合seed0先按五类计算成功回合均值，再做五类等权平均得到
  `B_ref,mixed`；每类成功率至少0.95。
- seed0不估计训练seed方差，报告必须说明；seed1–5、holdout和另两方法不得参与B_ref。

## 4. seed×地形指标

主指标为联合合格率，分母是该seed×地形全部回合。次指标包括：

- 成功率，未完整20 s、非法终止、路线预警和摆动足越界回合数；
- 成功回合的速度平均绝对误差、速度RMS（均方根）和跟踪RMSE（均方根误差）；
- 成功回合总、电气、机械和重力势能及分项残差；
- 成功回合`E_t/B_ref,t`、`E/净前进距离`和`E/实际水平路径`；距离非正时记缺失；
- 成功回合协调性；失败回合数及能耗均值、样本标准差、最小和最大值。

J/m是固定20 s数据的描述指标，不参与成功或联合合格。

## 5. 聚合和比较

先在每个PPO seed内聚合回合，再对seed1–5等权平均；seed间离散度用样本标准差`n−1`。
某seed无成功回合时，相应成功回合指标不可计算，不得跳过该seed。

相对任务型PPO节能只在同地形、同PPO seed内计算：
`1 − 当前方法成功回合平均能耗 / 任务型PPO成功回合平均能耗`。缺少任一均值时记不可计算。
禁止直接用不同地形原始焦耳比较节能。

跨地形先得到每类seed等权结果，再对五类等权宏平均；同时报告联合合格率最低的最差地形。
楼梯和斜坡分别报告上/下方向，五个难度层分别统计。即使阶段二各类样本数相同，也保持
宏平均顺序。

## 6. 三方法和防泄漏

三方法均进入seed1–5正式比较。固定权重明确标为PACE论文`W100=-0.00016`而非本地最优。
PACE-ECO报告绝对预算、对偶变量和违规率。样本仅5个训练seed，不预注册显著性检验；主要
报告配对原始值、均值、样本标准差、范围和效应方向。

holdout只有在全部对应`model_2999.pt`、配置和哈希齐备后由冻结工具授权，工具不得读取
性能。启封后不得修改任务、地形范围、seed、B80、固定权重、成功定义或聚合规则。发现
实现错误时保留证据并建立新版本，不能静默覆盖。
